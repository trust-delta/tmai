{
  "openapi": "3.1.0",
  "info": {
    "title": "tmai Web API",
    "description": "OpenAPI document for the tmai contract surface. REST path coverage is still PoC scope (#446); the schema section additionally enumerates the `CoreEvent` union and every referenced payload type so `tmai-xtask schema` can emit `corevents.schema.json` from the same derives.",
    "contact": {
      "name": "TrustDelta"
    },
    "license": {
      "name": "MIT",
      "identifier": "MIT"
    },
    "version": "3.0.1"
  },
  "paths": {
    "/api/agents/{id}/subscribe-terminal": {
      "post": {
        "tags": [
          "terminal"
        ],
        "summary": "Documentation stub for `POST /api/agents/{id}/subscribe-terminal`.",
        "description": "Real handler lives in the binary crate (`crate::web::api::subscribe_terminal`).\nIssues a short-lived terminal-plane ticket bound to the resolved agent;\nUI presents the ticket on the PTY-server's terminal-plane socket\n(rev3 Δ5). Browser UIs reach the listener through the WebSocket-to-Unix\nbridge whose path is returned in `stream_endpoint`; native UIs may\nconnect to the local `terminal.sock` directly.",
        "operationId": "subscribe_terminal_doc",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Agent identifier — canonical AgentId, target, pty_session_id, or semantic (issue:N / pr:N)",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Subscription ticket issued",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TerminalSubscription"
                }
              }
            }
          },
          "404": {
            "description": "Agent not found"
          },
          "503": {
            "description": "PTY-server not connected"
          }
        }
      }
    },
    "/api/github/pr/diff": {
      "get": {
        "tags": [
          "prs"
        ],
        "summary": "Documentation stub for `GET /api/github/pr/diff`.",
        "description": "Stage-1 in-tmai dev-loop (DR §A/§B): a PR's raw unified patch\n(`gh pr diff <n> --patch`) so the operator reviews the code diff\nin-tmai instead of on github.com. Verbatim diff text, not a parsed\nAST. Same `repo` + `pr_number` query shape and 30s TTL-cache\ndiscipline as the neighbouring `GET /api/github/pr/files`.",
        "operationId": "get_pr_diff_doc",
        "parameters": [
          {
            "name": "repo",
            "in": "query",
            "description": "Absolute repo path (a `repo_path` from `GET /api/units/{unit}/prs`)",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "pr_number",
            "in": "query",
            "description": "PR number within that repo",
            "required": true,
            "schema": {
              "type": "integer",
              "format": "int64",
              "minimum": 0
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Raw unified patch for the PR",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PrDiffResponse"
                }
              }
            }
          },
          "404": {
            "description": "Repository not found"
          },
          "500": {
            "description": "gh pr diff failed (is gh authenticated?)"
          }
        }
      }
    },
    "/api/github/pr/producer-reviewed": {
      "post": {
        "tags": [
          "prs"
        ],
        "summary": "Documentation stub for `POST /api/github/pr/producer-reviewed`.",
        "description": "dev-loop DR `2026-05-16-dev-loop-completes-in-tmai` §E (approach\n`2026-05-17-producer-review-gated-in-tmai-merge`): mark the Producer's\ncontract-anchored Δ-brief as *delivered* for a PR. Idempotent. Sets a\ntransient, in-memory delivered-marker only — a **neutral bit, NOT a\nmerge approval** (the Producer's opinion is *content of the brief*,\nnever this flag). Core never blocks a merge on it; the soft-valve\ndialog / colour / dismissible override are the react half's, a separate\ndispatch. Vendor-neutral. The marker surfaces on\n`GET /api/units/{unit}/prs` as `PrSummaryWire.producer_reviewed`.",
        "operationId": "mark_pr_producer_reviewed_doc",
        "responses": {
          "200": {
            "description": "Δ-brief marked delivered for the PR (idempotent)"
          },
          "404": {
            "description": "Repository not found"
          }
        }
      }
    },
    "/api/task-meta": {
      "get": {
        "tags": [
          "task-meta"
        ],
        "summary": "Documentation stub for `GET /api/task-meta`.",
        "description": "utoipa's path macro only needs a function *signature* to attach metadata;\nthe real handler is `crate::web::api::get_task_meta` in the binary crate.\nA follow-up will adopt `utoipa-axum` so handler and doc share one source.",
        "operationId": "get_task_meta_doc",
        "responses": {
          "200": {
            "description": "Task metadata entries for the active project",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/TaskMetaSnapshot"
                  }
                }
              }
            }
          }
        }
      }
    },
    "/api/units/{unit}/approaches": {
      "get": {
        "tags": [
          "approaches"
        ],
        "summary": "Documentation stub for `GET /api/units/{unit}/approaches`.",
        "description": "JSON projection of the `▣ Active approaches` section of the unit's\nhand-over per `doc/decisions/2026-05-15-records-architecture-two-axes.md`:\nevery `doc/approaches/*.md` record with `status: active`, grouped per\nrepo, with `serves:`, `success-signal`, `failure-signal`, and the\n`review-trigger` list surfaced as static text (mechanical trigger\ndetection lives in a follow-up PR per the trigger-semantics thread on\ntmai-core#361). Validated / rejected / replaced records are parsed\n(audit-trail preservation) but excluded from this view.",
        "operationId": "get_approaches_doc",
        "parameters": [
          {
            "name": "unit",
            "in": "path",
            "description": "Unit name (a configured `[[unit]]` or cwd-synthesized basename)",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Active approaches projection — bucketed per repo",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ApproachesResponse"
                }
              }
            }
          },
          "404": {
            "description": "Unit not found"
          },
          "500": {
            "description": "Gather (approach parse) failed"
          }
        }
      }
    },
    "/api/units/{unit}/calibration": {
      "get": {
        "tags": [
          "calibration"
        ],
        "summary": "Documentation stub for `GET /api/units/{unit}/calibration`.",
        "description": "Read-only projection of `<data_dir>/feedback/<unit>/calibration-store.md`.\nThe human's escape hatch into Producer hit-rate per DR §B.3; the Producer\nitself is blind by default. Polling this endpoint is enough to drive a\nWebUI top-bar tripwire indicator without depending on hand-over compose\ntiming — DR §B.4 zero-tolerance violations land in `tier1_violations`.",
        "operationId": "get_calibration_doc",
        "parameters": [
          {
            "name": "unit",
            "in": "path",
            "description": "Unit name (a configured `[[unit]]` or cwd-synthesized basename)",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "days",
            "in": "query",
            "description": "Time window in days; `0` = whole store. Default 90.",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "minimum": 0
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Calibration view + tier-1 tripwire list",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CalibrationResponse"
                }
              }
            }
          },
          "404": {
            "description": "Unit not found"
          },
          "500": {
            "description": "Calibration store load failed"
          }
        }
      }
    },
    "/api/units/{unit}/decisions": {
      "get": {
        "tags": [
          "decisions"
        ],
        "summary": "Documentation stub for `GET /api/units/{unit}/decisions`.",
        "description": "JSON projection of the `Settled` section of the unit's hand-over —\nbuckets (`📌 foundations` / `🔴 in_play` / `🟡 warm` / `⚪ cold` /\n`superseded`) plus the `⚠ Currency sweep` and `⚠ Trajectory check due`\ncallouts, per repo in the unit. Surfaces the same content\n`compose()` renders for the Producer's session-start digest, in a\nshape the React WebUI can consume directly.",
        "operationId": "get_decisions_doc",
        "parameters": [
          {
            "name": "unit",
            "in": "path",
            "description": "Unit name (a configured `[[unit]]` or cwd-synthesized basename)",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Settled decisions projection — bucketed per repo",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DecisionsResponse"
                }
              }
            }
          },
          "404": {
            "description": "Unit not found"
          },
          "500": {
            "description": "Gather (decision parse / git log) failed"
          }
        }
      }
    },
    "/api/units/{unit}/prs": {
      "get": {
        "tags": [
          "prs"
        ],
        "summary": "Documentation stub for `GET /api/units/{unit}/prs`.",
        "description": "Stage-1 in-tmai dev-loop (DR `2026-05-16-dev-loop-completes-in-tmai.md`\n§A): one unit-scoped list of the *open* PRs across every repo in the\nunit's `repos[]`, each PR tagged with the repo it belongs to (path +\nstable label + primary flag). Reuses the single-repo `GET /api/github/prs`\ngh-CLI path (30s TTL cache) fanned out over the unit's repos. Unit\nresolution goes through the post-#387 resolver, so a worktree-name unit\nstill reduces to its owning unit.",
        "operationId": "get_unit_prs_doc",
        "parameters": [
          {
            "name": "unit",
            "in": "path",
            "description": "Unit name (a configured `[[unit]]` or cwd-synthesized basename)",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Open PRs across the unit's repos, grouped per repo",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UnitPrsResponse"
                }
              }
            }
          },
          "404": {
            "description": "Unit not found"
          },
          "500": {
            "description": "PR listing failed for a repo (is gh authenticated?)"
          }
        }
      }
    },
    "/api/units/{unit}/working-with-human": {
      "get": {
        "tags": [
          "working-with-human"
        ],
        "summary": "Documentation stub for `GET /api/units/{unit}/working-with-human`.",
        "description": "JSON projection of the `◐ Working with this human` section: the\nresolved memory directory plus the raw `MEMORY.md` contents.\nSurfaces the same content the Producer reads on session-start so\nthe WebUI's fourth digest section can render the index without a\nProducer being attached.\n\n`dir = None` ⇒ no memory dir resolves for this unit (no override,\nno auto-derived `~/.claude/projects/<slug>/memory`). `dir = Some` +\n`memory_index = None` ⇒ the directory exists but `MEMORY.md` is\nabsent. Both are honest empty-states, not errors.",
        "operationId": "get_working_with_human_doc",
        "parameters": [
          {
            "name": "unit",
            "in": "path",
            "description": "Unit name (a configured `[[unit]]` or cwd-synthesized basename)",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Working-with-human projection — memory dir + MEMORY.md",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkingWithHumanResponse"
                }
              }
            }
          },
          "404": {
            "description": "Unit not found"
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "ActionOrigin": {
        "oneOf": [
          {
            "type": "object",
            "description": "A human interacting through a UI surface",
            "required": [
              "interface",
              "kind"
            ],
            "properties": {
              "cwd": {
                "type": [
                  "string",
                  "null"
                ],
                "description": "Working directory of the UI request, used for project-scope\nattribution of the action (#89). The WebUI/TUI passes the cwd from\nthe request body so a server-side `ActionPerformed` carries its\noriginating project even when the caller is not tracked in\n`state.agents`."
              },
              "interface": {
                "type": "string",
                "description": "Which interface: \"webui\", \"tui\", \"mobile\", \"cli\", etc."
              },
              "kind": {
                "type": "string",
                "enum": [
                  "Human"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "An AI agent (orchestrator or worker) via MCP tools",
            "required": [
              "id",
              "kind"
            ],
            "properties": {
              "cwd": {
                "type": [
                  "string",
                  "null"
                ],
                "description": "Caller working directory at origin creation time (MCP loopback only).\n\nSet by `TmaiHttpClient::from_runtime` via `std::env::current_dir()`\nso a server-side `ActionPerformed` carries its originating project\neven when the MCP agent ID is not tracked in `state.agents` (#75)."
              },
              "id": {
                "type": "string",
                "description": "Canonical AgentId in `<scheme>:<id>` form (e.g. `\"claude:ea760770-c137-...\"`),\nmatching `AgentSnapshot.id`."
              },
              "is_producer": {
                "type": "boolean",
                "description": "Whether this agent is the Producer.\n\nWire field name `is_producer` (DR\n`2026-05-16-producer-identity-and-operator-addressing` §B —\nthe deliberate contract-surface change that #402's neutral\n`is_orchestrator` rename was holding back). `default` +\n`skip_serializing_if` make it optional: an old client that\nomits the field still deserializes (→ `false`) and the field\nis absent on the wire when `false`, so neither side is forced\ninto lockstep."
              },
              "kind": {
                "type": "string",
                "enum": [
                  "Agent"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "An automated system process (auto_cleanup, pr_monitor, etc.)",
            "required": [
              "subsystem",
              "kind"
            ],
            "properties": {
              "cwd": {
                "type": [
                  "string",
                  "null"
                ],
                "description": "Working directory of the originating subsystem process, used for\nproject-scope routing (#89)."
              },
              "kind": {
                "type": "string",
                "enum": [
                  "System"
                ]
              },
              "subsystem": {
                "type": "string",
                "description": "Subsystem name (e.g., \"auto_cleanup\", \"pr_monitor\")"
              }
            }
          }
        ],
        "description": "Origin of an API action — tracks who initiated the operation.\n\nUsed by the orchestrator notification middleware to provide context\nabout what triggered a side-effect API call."
      },
      "AgentAttention": {
        "type": "string",
        "description": "What the agent is waiting for. Three variants; absence (`Option::None`)\nmeans \"running normally — no UI signal needed\".\n\nHint-only for UI / audit consumers; Core / PTY-server / Hub must never\nbranch on the discriminant (decision §3 wire contract from the\nattention rebuild, preserved by the 2026-05-09 enum simplification).",
        "enum": [
          "started",
          "halted",
          "completed"
        ]
      },
      "ApproachStatus": {
        "type": "string",
        "description": "Lifecycle position of an approach record.\n\nUnlike [`super::decision::DecisionStatus`], the approach lifecycle resolves\noutward (`active → validated / rejected / replaced`) rather than through a\nsupersede chain. A validated / rejected approach is preserved as the\naudit-trail record of *what we tried and what happened*; a follow-up\ndecision (if any) is authored separately rather than in-place.",
        "enum": [
          "active",
          "validated",
          "rejected",
          "replaced"
        ]
      },
      "ApproachWire": {
        "type": "object",
        "description": "One approach projected to the wire — the human-side complement of the\ninternal [`crate::workbench::compose::ApproachView`].",
        "required": [
          "slug",
          "title",
          "date",
          "status",
          "governs",
          "serves",
          "success_signal",
          "failure_signal",
          "review_triggers",
          "review_history",
          "replaced_by",
          "excerpt"
        ],
        "properties": {
          "confidence": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Confidence",
                "description": "Producer confidence; `None` when no confidence is on record."
              }
            ]
          },
          "date": {
            "type": "string",
            "description": "ISO-8601 date parsed from the slug's `YYYY-MM-DD-` prefix — the\ncreation date. Approaches don't carry a `last_verified` field;\nre-evaluation is signal-driven via `review_triggers`."
          },
          "excerpt": {
            "type": "string",
            "description": "First-paragraph summary of the body."
          },
          "failure_signal": {
            "type": "string"
          },
          "governs": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "replaced_by": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Approach slugs that replaced this one — meaningful only when\n`status: replaced`."
          },
          "review_history": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ReviewExtensionWire"
            },
            "description": "Extend ledger — empty for fresh approaches."
          },
          "review_triggers": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ReviewTriggerWire"
            },
            "description": "Re-evaluation triggers, non-empty per parse-time validation. The\nsingle-date `review_after:` sugar is already expanded into a `Date`\ntrigger by the parser."
          },
          "serves": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "`serves:` — non-empty per parse-time validation."
          },
          "slug": {
            "type": "string"
          },
          "status": {
            "$ref": "#/components/schemas/ApproachStatus"
          },
          "success_signal": {
            "type": "string"
          },
          "title": {
            "type": "string"
          }
        }
      },
      "ApproachesResponse": {
        "type": "object",
        "description": "`GET /api/units/{unit}/approaches` response — the `▣ Active approaches`\nsection of the unit's hand-over, per repo, JSON-projected. Composed on\ndemand (no cache).\n\nFor a single-repo unit this collapses to `repos.len() == 1` — the\ncommon case until per-unit `also[]` per-repo `approaches_dir`\noverrides land (tracked in tmai-core#340 for the multi-repo follow-up\nalongside the decisions-side override).",
        "required": [
          "unit",
          "composed_at",
          "repos"
        ],
        "properties": {
          "composed_at": {
            "type": "string",
            "description": "RFC3339 compose timestamp."
          },
          "repos": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RepoApproachesWire"
            }
          },
          "unit": {
            "type": "string"
          }
        }
      },
      "BundleId": {
        "type": "string",
        "description": "Stable identifier for a dispatch bundle (a logical grouping of\nsub-dispatches — e.g. \"apply the same change across 4 repos\").\n\nSerialized transparently as a plain string so that existing log and\nevent consumers can treat the field as an opaque identifier without\nneeding to know the wrapper type."
      },
      "BundleStatus": {
        "type": "string",
        "description": "Rollup status of a dispatch bundle, emitted by\n[`CoreEvent::BundleStatusChanged`].\n\nDerived from its sub-dispatches; transitions here are coarser-grained\nthan the per-agent `AgentStatusChanged` stream (issue #463, open\nquestion 3).",
        "enum": [
          "pending",
          "running",
          "partially_completed",
          "completed",
          "failed",
          "cancelled"
        ]
      },
      "CalibrationCellWire": {
        "type": "object",
        "description": "One row of the calibration table, flattened from the workbench's\n`BTreeMap<(verdict, confidence), Cell>` for JSON serialization.\n\nJSON cannot key on a tuple; the rendered shape is a list of objects so\nthe React client iterates without having to reconstruct the map shape.",
        "required": [
          "verdict",
          "confidence",
          "n",
          "hits",
          "misses"
        ],
        "properties": {
          "confidence": {
            "$ref": "#/components/schemas/Confidence"
          },
          "hits": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "misses": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "n": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "verdict": {
            "$ref": "#/components/schemas/TriageVerdict"
          }
        }
      },
      "CalibrationEntry": {
        "type": "object",
        "description": "One entry in the calibration store: a single triage decision plus its\nlater-observed objective outcome (or none yet).",
        "required": [
          "synthesis_pass_id",
          "note_source",
          "verdict",
          "confidence",
          "tier_routed",
          "rationale",
          "recorded_at"
        ],
        "properties": {
          "confidence": {
            "$ref": "#/components/schemas/Confidence"
          },
          "note_source": {
            "type": "string",
            "description": "Source the verdict came from, e.g. a worker friction note filename\n(`20260513-1500-baz.md`) or a drift-audit item slug. Relative to the\nunit's feedback directory; opaque to this module."
          },
          "outcome": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Outcome",
                "description": "Objective outcome, if one has been picked up since (stage 5).\nAbsent in fresh entries; serialized only when present."
              }
            ]
          },
          "rationale": {
            "type": "string",
            "description": "One-line rationale from the Producer, for human review later."
          },
          "recorded_at": {
            "type": "string",
            "format": "date-time",
            "description": "When the entry was appended."
          },
          "synthesis_pass_id": {
            "type": "string",
            "description": "UUID of the synthesis pass that produced this verdict. String, not\n`uuid::Uuid`, so older entries with non-UUID ids stay parseable."
          },
          "tier_routed": {
            "type": "integer",
            "format": "int32",
            "description": "Tier the verdict was routed to (`1` = human-gated tripwire, `2` =\nProducer may handle). Matches `DecisionFrontmatter::tier`.",
            "minimum": 0
          },
          "verdict": {
            "$ref": "#/components/schemas/TriageVerdict"
          }
        }
      },
      "CalibrationResponse": {
        "type": "object",
        "description": "`GET /api/units/{unit}/calibration` response — read-only projection of\nthe unit's calibration store.\n\n`bootstrap_threshold` mirrors `workbench::BOOTSTRAP_ENTRY_THRESHOLD` so\nthe client can show the \"lean toward asking the human\" caveat without\nre-encoding the constant. `tier1_violations` mirrors what the hand-over\ndigest's `⚡ TIER-1 TRIPWIRE TRIGGERED` callout surfaces (DR §B.4 zero\ntolerance) — so polling this endpoint is enough to drive a top-bar\ntripwire indicator in the WebUI without depending on hand-over compose\ntiming.",
        "required": [
          "unit",
          "days",
          "total_in_store",
          "total_in_window",
          "bootstrap_threshold",
          "cells",
          "tier1_routed",
          "tier1_violations",
          "recent_false_negatives"
        ],
        "properties": {
          "bootstrap_threshold": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "cells": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CalibrationCellWire"
            }
          },
          "days": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "recent_false_negatives": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CalibrationEntry"
            }
          },
          "tier1_routed": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "tier1_violations": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CalibrationEntry"
            }
          },
          "total_in_store": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "total_in_window": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "unit": {
            "type": "string"
          }
        }
      },
      "CapacityCauseSummary": {
        "oneOf": [
          {
            "type": "object",
            "description": "An agent was spawned; consumed a slot.",
            "required": [
              "target",
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "agent_spawned"
                ]
              },
              "target": {
                "type": "string",
                "description": "Target of the newly spawned agent."
              }
            }
          },
          {
            "type": "object",
            "description": "An agent reached a terminal state (exit/kill); freed a slot.",
            "required": [
              "target",
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "agent_terminated"
                ]
              },
              "target": {
                "type": "string",
                "description": "Target of the agent that ended."
              }
            }
          },
          {
            "type": "object",
            "description": "The configured capacity limit changed (reload / admin action).",
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "limit_changed"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Capacity snapshot was reconciled against live state (drift fix).",
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "reconciled"
                ]
              }
            }
          }
        ],
        "description": "What caused a [`CoreEvent::CapacityChanged`] emission.\n\nSubscribers use this to attribute the capacity delta without needing\na separate query. `delta == 0` is valid (e.g. `LimitChanged` when a\nconfig reload moved the ceiling but no slots moved)."
      },
      "CapacityInfo": {
        "type": "object",
        "description": "Current agent capacity counters.",
        "required": [
          "current",
          "limit",
          "remaining"
        ],
        "properties": {
          "current": {
            "type": "integer",
            "format": "int32",
            "description": "Number of agent slots currently in use.",
            "minimum": 0
          },
          "limit": {
            "type": "integer",
            "format": "int32",
            "description": "Configured maximum concurrent agents.",
            "minimum": 0
          },
          "remaining": {
            "type": "integer",
            "format": "int32",
            "description": "Remaining available slots (`limit - current`, saturating).",
            "minimum": 0
          }
        }
      },
      "Confidence": {
        "type": "string",
        "description": "The Producer's own confidence in a triage verdict. Binary by DR §B.1 —\nwhen in doubt, [`Confidence::Low`].\n\n`Ord` is derived (lexicographic on the variant order: `High` < `Low`) so\nit can be a `BTreeMap` key for stable table rendering — order has no\nnumeric meaning. Same reasoning for [`TriageVerdict`] below.",
        "enum": [
          "high",
          "low"
        ]
      },
      "CoreEvent": {
        "oneOf": [
          {
            "type": "object",
            "description": "A team member became idle (waiting for next task)",
            "required": [
              "target",
              "team_name",
              "member_name",
              "type"
            ],
            "properties": {
              "member_name": {
                "type": "string",
                "description": "Member name"
              },
              "target": {
                "type": "string",
                "description": "Agent target ID"
              },
              "team_name": {
                "type": "string",
                "description": "Team name"
              },
              "type": {
                "type": "string",
                "enum": [
                  "TeammateIdle"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A new task was created",
            "required": [
              "team_name",
              "task_id",
              "task_subject",
              "type"
            ],
            "properties": {
              "task_id": {
                "type": "string",
                "description": "Task ID"
              },
              "task_subject": {
                "type": "string",
                "description": "Task subject"
              },
              "team_name": {
                "type": "string",
                "description": "Team name"
              },
              "type": {
                "type": "string",
                "enum": [
                  "TaskCreated"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A task was completed",
            "required": [
              "team_name",
              "task_id",
              "task_subject",
              "type"
            ],
            "properties": {
              "task_id": {
                "type": "string",
                "description": "Task ID"
              },
              "task_subject": {
                "type": "string",
                "description": "Task subject"
              },
              "team_name": {
                "type": "string",
                "description": "Team name"
              },
              "type": {
                "type": "string",
                "enum": [
                  "TaskCompleted"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Claude Code configuration file was changed",
            "required": [
              "target",
              "source",
              "file_path",
              "type"
            ],
            "properties": {
              "file_path": {
                "type": "string",
                "description": "Changed file path"
              },
              "source": {
                "type": "string",
                "description": "Config source (e.g., \"user_settings\", \"project_settings\")"
              },
              "target": {
                "type": "string",
                "description": "Agent target ID"
              },
              "type": {
                "type": "string",
                "enum": [
                  "ConfigChanged"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "CLAUDE.md or `.claude/rules/*.md` files were loaded into context\n\nAdded in Claude Code v2.1.69. Fires when instruction files are loaded.",
            "required": [
              "target",
              "type"
            ],
            "properties": {
              "target": {
                "type": "string",
                "description": "Agent target ID"
              },
              "type": {
                "type": "string",
                "enum": [
                  "InstructionsLoaded"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "An agent stopped (completed or paused), emitted by hook handler",
            "required": [
              "target",
              "cwd",
              "type"
            ],
            "properties": {
              "cwd": {
                "type": "string",
                "description": "Working directory"
              },
              "last_assistant_message": {
                "type": [
                  "string",
                  "null"
                ],
                "description": "Last assistant message (if available)"
              },
              "target": {
                "type": "string",
                "description": "Agent target ID"
              },
              "type": {
                "type": "string",
                "enum": [
                  "AgentStopped"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "An agent's `AgentId` was promoted from `Provisional` to a native\nsession id once the underlying CLI reported one (Phase 1 of #96).\n\nSubscribers that hold references to the previous id should rewrite\nthem to `to`. The wire form of `from` and `to` is `<scheme>:<id>`\n(e.g. `\"claude:ea760770-...\"`). `target` is the tmux pane id and\nremains the legacy lookup key during Phase 1.",
            "required": [
              "target",
              "from",
              "to",
              "type"
            ],
            "properties": {
              "from": {
                "type": "string",
                "description": "Previous id, always a `provisional:<uuid>` in Phase 1"
              },
              "target": {
                "type": "string",
                "description": "Agent target ID (tmux pane string)"
              },
              "to": {
                "type": "string",
                "description": "New native id (`claude:<uuid>` / `codex:<uuid>` / etc.)"
              },
              "type": {
                "type": "string",
                "enum": [
                  "AgentIdPromoted"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Context compaction started (PreCompact hook event)",
            "required": [
              "target",
              "compaction_count",
              "type"
            ],
            "properties": {
              "compaction_count": {
                "type": "integer",
                "format": "int32",
                "description": "How many compactions have occurred in this session",
                "minimum": 0
              },
              "target": {
                "type": "string",
                "description": "Agent target ID"
              },
              "type": {
                "type": "string",
                "enum": [
                  "ContextCompacting"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Worktree setup commands completed successfully",
            "required": [
              "worktree_path",
              "branch",
              "type"
            ],
            "properties": {
              "branch": {
                "type": "string",
                "description": "Branch name"
              },
              "type": {
                "type": "string",
                "enum": [
                  "WorktreeSetupCompleted"
                ]
              },
              "worktree_path": {
                "type": "string",
                "description": "Absolute path to the worktree"
              }
            }
          },
          {
            "type": "object",
            "description": "Worktree setup commands failed",
            "required": [
              "worktree_path",
              "branch",
              "error",
              "type"
            ],
            "properties": {
              "branch": {
                "type": "string",
                "description": "Branch name"
              },
              "error": {
                "type": "string",
                "description": "Error message"
              },
              "type": {
                "type": "string",
                "enum": [
                  "WorktreeSetupFailed"
                ]
              },
              "worktree_path": {
                "type": "string",
                "description": "Absolute path to the worktree"
              }
            }
          },
          {
            "type": "object",
            "description": "A queued prompt is ready to be delivered (agent transitioned to Idle)",
            "required": [
              "target",
              "prompt",
              "type"
            ],
            "properties": {
              "prompt": {
                "type": "string",
                "description": "The prompt text to send"
              },
              "target": {
                "type": "string",
                "description": "Agent target ID"
              },
              "type": {
                "type": "string",
                "enum": [
                  "PromptReady"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Usage data was updated (after a fetch cycle)",
            "required": [
              "type"
            ],
            "properties": {
              "type": {
                "type": "string",
                "enum": [
                  "UsageUpdated"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A tool call was deferred for external resolution",
            "required": [
              "defer_id",
              "target",
              "tool_name",
              "type"
            ],
            "properties": {
              "defer_id": {
                "type": "integer",
                "format": "int64",
                "description": "Unique deferred call ID",
                "minimum": 0
              },
              "target": {
                "type": "string",
                "description": "Agent target/pane ID"
              },
              "tool_name": {
                "type": "string",
                "description": "Tool name"
              },
              "type": {
                "type": "string",
                "enum": [
                  "ToolCallDeferred"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A worktree branch was successfully rebased onto the default branch",
            "required": [
              "branch",
              "worktree_path",
              "type"
            ],
            "properties": {
              "branch": {
                "type": "string",
                "description": "Branch that was rebased"
              },
              "type": {
                "type": "string",
                "enum": [
                  "RebaseSucceeded"
                ]
              },
              "worktree_path": {
                "type": "string",
                "description": "Worktree path"
              }
            }
          },
          {
            "type": "object",
            "description": "A worktree branch rebase failed due to conflicts",
            "required": [
              "branch",
              "worktree_path",
              "error",
              "type"
            ],
            "properties": {
              "branch": {
                "type": "string",
                "description": "Branch that had conflicts"
              },
              "error": {
                "type": "string",
                "description": "Error details"
              },
              "type": {
                "type": "string",
                "enum": [
                  "RebaseConflict"
                ]
              },
              "worktree_path": {
                "type": "string",
                "description": "Worktree path"
              }
            }
          },
          {
            "type": "object",
            "description": "A deferred tool call was resolved (approved/denied)",
            "required": [
              "defer_id",
              "target",
              "decision",
              "resolved_by",
              "type"
            ],
            "properties": {
              "decision": {
                "type": "string",
                "description": "Resolution: \"allow\" or \"deny\""
              },
              "defer_id": {
                "type": "integer",
                "format": "int64",
                "description": "Unique deferred call ID",
                "minimum": 0
              },
              "resolved_by": {
                "type": "string",
                "description": "Who resolved it (e.g., \"human\", \"ai:haiku\", \"timeout\")"
              },
              "target": {
                "type": "string",
                "description": "Agent target/pane ID"
              },
              "type": {
                "type": "string",
                "enum": [
                  "ToolCallResolved"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A new PR was detected by the PR monitor",
            "required": [
              "pr_number",
              "title",
              "branch",
              "type"
            ],
            "properties": {
              "branch": {
                "type": "string",
                "description": "Head branch name"
              },
              "pr_number": {
                "type": "integer",
                "format": "int64",
                "description": "PR number",
                "minimum": 0
              },
              "title": {
                "type": "string",
                "description": "PR title"
              },
              "type": {
                "type": "string",
                "enum": [
                  "PrCreated"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "CI checks passed for a PR",
            "required": [
              "pr_number",
              "title",
              "checks_summary",
              "type"
            ],
            "properties": {
              "checks_summary": {
                "type": "string",
                "description": "Summary of passed checks"
              },
              "pr_number": {
                "type": "integer",
                "format": "int64",
                "description": "PR number",
                "minimum": 0
              },
              "title": {
                "type": "string",
                "description": "PR title"
              },
              "type": {
                "type": "string",
                "enum": [
                  "PrCiPassed"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "CI checks failed for a PR",
            "required": [
              "pr_number",
              "title",
              "failed_details",
              "type"
            ],
            "properties": {
              "failed_details": {
                "type": "string",
                "description": "Details of failed checks"
              },
              "pr_number": {
                "type": "integer",
                "format": "int64",
                "description": "PR number",
                "minimum": 0
              },
              "title": {
                "type": "string",
                "description": "PR title"
              },
              "type": {
                "type": "string",
                "enum": [
                  "PrCiFailed"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A PR received review feedback (changes requested)",
            "required": [
              "pr_number",
              "title",
              "comments_summary",
              "type"
            ],
            "properties": {
              "comments_summary": {
                "type": "string",
                "description": "Summary of review comments"
              },
              "pr_number": {
                "type": "integer",
                "format": "int64",
                "description": "PR number",
                "minimum": 0
              },
              "review_count": {
                "type": "integer",
                "format": "int64",
                "description": "Monotonic review count from PR state — distinguishes\nback-to-back ChangesRequested transitions so the notifier\ncan dedupe on `(pr_number, review_count)` instead of\ncollapsing distinct rounds into one entry.",
                "minimum": 0
              },
              "title": {
                "type": "string",
                "description": "PR title"
              },
              "type": {
                "type": "string",
                "enum": [
                  "PrReviewFeedback"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A PR was closed (merged or closed without merging)",
            "required": [
              "pr_number",
              "title",
              "branch",
              "type"
            ],
            "properties": {
              "branch": {
                "type": "string",
                "description": "Head branch name"
              },
              "pr_number": {
                "type": "integer",
                "format": "int64",
                "description": "PR number",
                "minimum": 0
              },
              "title": {
                "type": "string",
                "description": "PR title"
              },
              "type": {
                "type": "string",
                "enum": [
                  "PrClosed"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A PR has been open without any CI run starting for longer than\n`OrchestrationSettings::pr_monitor_ci_start_timeout_secs`. Typically\nindicates GitHub Actions silently skipped (merge conflicts blocking\nCI, Actions disabled at the repo, etc.) — see #334. Counted from the\nfirst time the monitor observed the PR; warm-up baseline PRs are\nexempt so a tmai restart does not retro-fire the alarm. Emitted at\nmost once per PR.",
            "required": [
              "pr_number",
              "title",
              "branch",
              "waited_secs",
              "type"
            ],
            "properties": {
              "branch": {
                "type": "string",
                "description": "Head branch name"
              },
              "pr_number": {
                "type": "integer",
                "format": "int64",
                "description": "PR number",
                "minimum": 0
              },
              "title": {
                "type": "string",
                "description": "PR title"
              },
              "type": {
                "type": "string",
                "enum": [
                  "PrCiStartTimeout"
                ]
              },
              "waited_secs": {
                "type": "integer",
                "format": "int64",
                "description": "How many seconds the monitor has been watching this PR with no\nCI status. Always >= the configured timeout.",
                "minimum": 0
              }
            }
          },
          {
            "type": "object",
            "description": "Git state changed for a repository (branches, HEAD, commit graph).\n\nEmitted by [`crate::git::monitor::GitMonitor`] when its poll detects\na transition. WebUI subscribers refetch `/api/git/*` in response so\nthe UI and the monitor snapshot observe the same tick (#423 — sibling\nof the PR Monitor SoT pattern).",
            "required": [
              "repo",
              "type"
            ],
            "properties": {
              "repo": {
                "type": "string",
                "description": "Repository directory path"
              },
              "type": {
                "type": "string",
                "enum": [
                  "GitStateChanged"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "An agent's tmux target changed due to pane renumbering (PID-based reconciliation)",
            "required": [
              "old_target",
              "new_target",
              "pid",
              "type"
            ],
            "properties": {
              "new_target": {
                "type": "string",
                "description": "New target ID (e.g., \"main:0.1\")"
              },
              "old_target": {
                "type": "string",
                "description": "Previous target ID (e.g., \"main:0.2\")"
              },
              "pid": {
                "type": "integer",
                "format": "int32",
                "description": "Process ID that links the two targets",
                "minimum": 0
              },
              "type": {
                "type": "string",
                "enum": [
                  "AgentTargetChanged"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A side-effect API action was performed (for orchestrator notification)",
            "required": [
              "origin",
              "action",
              "summary",
              "type"
            ],
            "properties": {
              "action": {
                "type": "string",
                "description": "API action name (e.g., \"dispatch_issue\", \"kill_agent\", \"merge_pr\")"
              },
              "origin": {
                "$ref": "#/components/schemas/ActionOrigin",
                "description": "Who initiated the action"
              },
              "summary": {
                "type": "string",
                "description": "Human-readable summary of the action"
              },
              "type": {
                "type": "string",
                "enum": [
                  "ActionPerformed"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A dispatch request was rejected by the contract-layer gatekeeper\nbefore an agent was spawned. Subscribers never saw this intent as an\n`AgentAppeared` / `AgentStatusChanged` — they learn about it only via\nthis event. Payload is redacted: see [`DispatchIntentSummary`].",
            "required": [
              "intent_summary",
              "origin",
              "error",
              "type"
            ],
            "properties": {
              "error": {
                "$ref": "#/components/schemas/TmaiError",
                "description": "Structured reason for the rejection."
              },
              "intent_summary": {
                "$ref": "#/components/schemas/DispatchIntentSummary",
                "description": "Redacted description of what was dispatched."
              },
              "origin": {
                "$ref": "#/components/schemas/ActionOrigin",
                "description": "Who issued the rejected dispatch."
              },
              "type": {
                "type": "string",
                "enum": [
                  "DispatchRejected"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Vendor availability transitioned (rate-limit hit or cleared, outage\nstarted or resolved). Subscribers can refresh capacity badges or\nresume queued dispatches without polling the vendor directly.",
            "required": [
              "vendor",
              "old",
              "new",
              "detected_via",
              "type"
            ],
            "properties": {
              "account": {
                "type": [
                  "string",
                  "null"
                ],
                "description": "Account label, if the deployment distinguishes accounts."
              },
              "detected_via": {
                "$ref": "#/components/schemas/DetectionSource",
                "description": "How the transition was detected (hook, polling, explicit signal)."
              },
              "new": {
                "$ref": "#/components/schemas/VendorAvailabilityState",
                "description": "New availability state."
              },
              "old": {
                "$ref": "#/components/schemas/VendorAvailabilityState",
                "description": "Previous availability state."
              },
              "type": {
                "type": "string",
                "enum": [
                  "VendorAvailabilityChanged"
                ]
              },
              "vendor": {
                "type": "string",
                "description": "Vendor identifier (e.g. \"anthropic\", \"openai\", \"google\")."
              }
            }
          },
          {
            "type": "object",
            "description": "The global capacity counter moved (a slot was consumed or freed)\nor the configured limit was adjusted. `delta` is the change applied\nto `current`; `cause` attributes the change without requiring a\nfollow-up query.",
            "required": [
              "current",
              "limit",
              "delta",
              "cause",
              "type"
            ],
            "properties": {
              "cause": {
                "$ref": "#/components/schemas/CapacityCauseSummary",
                "description": "What triggered the change."
              },
              "current": {
                "type": "integer",
                "description": "New value of the in-use slot counter.",
                "minimum": 0
              },
              "delta": {
                "type": "integer",
                "format": "int32",
                "description": "Signed change applied to `current` by this event\n(+1 on spawn, -1 on terminal, 0 on `LimitChanged`/`Reconciled`)."
              },
              "limit": {
                "type": "integer",
                "description": "Configured ceiling at the time of emission.",
                "minimum": 0
              },
              "type": {
                "type": "string",
                "enum": [
                  "CapacityChanged"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A contract-layer invariant was violated (auth failure, schema\nmismatch, unauthorized operation, etc.). Distinct from\n`DispatchRejected` which is gatekeeper-specific (#463, open q. 4).",
            "required": [
              "origin",
              "code",
              "type"
            ],
            "properties": {
              "code": {
                "$ref": "#/components/schemas/ErrorCode",
                "description": "Machine-readable classification of the violation."
              },
              "context": {
                "type": "object",
                "description": "Code-specific structured context (never includes secrets or\nraw prompts). Defaults to `null` when the emitter has nothing\nto attach."
              },
              "origin": {
                "$ref": "#/components/schemas/ActionOrigin",
                "description": "Origin of the violating call."
              },
              "type": {
                "type": "string",
                "enum": [
                  "ContractViolation"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A bypass flag was used for a dispatch (skipped one or more\ncontract-layer checks). Always audit-logged; subscribers can\nuse this to surface a banner or alert.",
            "required": [
              "origin",
              "bypassed",
              "reason",
              "type"
            ],
            "properties": {
              "bypassed": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "Names of the bypassed checks / stages (e.g.\n\"capacity_check\", \"vendor_availability\")."
              },
              "origin": {
                "$ref": "#/components/schemas/ActionOrigin",
                "description": "Origin of the bypassing call."
              },
              "reason": {
                "type": "string",
                "description": "Free-text justification supplied by the caller."
              },
              "type": {
                "type": "string",
                "enum": [
                  "DispatchBypassUsed"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A single agent was upserted (appeared / changed state) or removed.",
            "required": [
              "change",
              "id",
              "type"
            ],
            "properties": {
              "change": {
                "$ref": "#/components/schemas/EntityChange",
                "description": "Whether the agent was upserted or removed."
              },
              "id": {
                "type": "string",
                "description": "Canonical AgentId (`<scheme>:<id>` form, matches `AgentSnapshot.id`).\nStable across tmux pane renumber, `claude --resume`, tmai restart, and\nprocess restart. Provisional ids (`provisional:<uuid>`) are normally\nhidden but become wire-visible when the agent has a `pty_session_id`\n(PTY-server backed spawn)."
              },
              "origin": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/ActionOrigin",
                    "description": "Who or what triggered this update."
                  }
                ]
              },
              "snapshot": {
                "type": "object",
                "description": "Serialized [`super::types::AgentSnapshot`]; `None` when removed."
              },
              "type": {
                "type": "string",
                "enum": [
                  "AgentUpdate"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A dispatch ledger entry was upserted or removed.\n\nEmitted alongside `DispatchSpawned` / `DispatchStateChanged` /\n`DispatchStopped` during Phase 1.",
            "required": [
              "change",
              "id",
              "type"
            ],
            "properties": {
              "change": {
                "$ref": "#/components/schemas/EntityChange",
                "description": "Whether the dispatch was upserted or removed."
              },
              "id": {
                "type": "string",
                "description": "Hyphenated dispatch id string."
              },
              "origin": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/ActionOrigin",
                    "description": "Who or what triggered this update."
                  }
                ]
              },
              "snapshot": {
                "type": "object",
                "description": "Serialized [`super::snapshot::DispatchSnapshot`]; `None` when removed."
              },
              "type": {
                "type": "string",
                "enum": [
                  "DispatchUpdate"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A team snapshot was upserted or removed.\n\nEmitted alongside `TeamsUpdated` during Phase 1.",
            "required": [
              "change",
              "name",
              "type"
            ],
            "properties": {
              "change": {
                "$ref": "#/components/schemas/EntityChange",
                "description": "Whether the team was upserted or removed."
              },
              "name": {
                "type": "string",
                "description": "Team name (primary key)."
              },
              "origin": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/ActionOrigin",
                    "description": "Who or what triggered this update."
                  }
                ]
              },
              "snapshot": {
                "type": "object",
                "description": "Serialized [`super::types::TeamSnapshot`]; `None` when removed."
              },
              "type": {
                "type": "string",
                "enum": [
                  "TeamUpdate"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A worktree snapshot was upserted or removed.\n\nEmitted alongside `WorktreeCreated` / `WorktreeRemoved` during Phase 1.",
            "required": [
              "change",
              "id",
              "type"
            ],
            "properties": {
              "change": {
                "$ref": "#/components/schemas/EntityChange",
                "description": "Whether the worktree was upserted or removed."
              },
              "id": {
                "type": "string",
                "description": "Worktree path (unique per repo)."
              },
              "origin": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/ActionOrigin",
                    "description": "Who or what triggered this update."
                  }
                ]
              },
              "snapshot": {
                "type": "object",
                "description": "Serialized [`super::types::WorktreeSnapshot`]; `None` when removed."
              },
              "type": {
                "type": "string",
                "enum": [
                  "WorktreeUpdate"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "The global prompt queue changed (enqueue / drain / cancel).\n\nEmitted alongside `PromptQueueChanged` during Phase 1.\nCarries the full [`super::types::QueueSnapshot`] so consumers can\nreplace their local snapshot directly.",
            "required": [
              "snapshot",
              "type"
            ],
            "properties": {
              "origin": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/ActionOrigin",
                    "description": "Who or what triggered this update."
                  }
                ]
              },
              "snapshot": {
                "type": "object",
                "description": "Serialized full [`super::types::QueueSnapshot`] after the mutation."
              },
              "type": {
                "type": "string",
                "enum": [
                  "QueueUpdate"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Runtime infrastructure state changed.\n\nEmitted when `CapacityChanged`, `VendorAvailabilityChanged`,\nor `ContextCompacting` mutate the runtime view.",
            "required": [
              "snapshot",
              "type"
            ],
            "properties": {
              "origin": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/ActionOrigin",
                    "description": "Who or what triggered this update."
                  }
                ]
              },
              "snapshot": {
                "type": "object",
                "description": "Serialized full [`super::snapshot::RuntimeSnapshot`]."
              },
              "type": {
                "type": "string",
                "enum": [
                  "RuntimeUpdate"
                ]
              }
            }
          },
          {
            "allOf": [
              {
                "$ref": "#/components/schemas/HandoffRitualEvent",
                "description": "A Producer handoff-and-restart ritual transitioned phases (DR\n`2026-05-14-handoff-lifecycle-and-kill-ux.md` §C/§F). Emitted by the\n`POST /api/units/{unit}/handoff-and-restart` endpoint on every step;\nstreamed to SSE consumers as event kind `handoff_ritual`."
              },
              {
                "type": "object",
                "required": [
                  "type"
                ],
                "properties": {
                  "type": {
                    "type": "string",
                    "enum": [
                      "HandoffRitual"
                    ]
                  }
                }
              }
            ],
            "description": "A Producer handoff-and-restart ritual transitioned phases (DR\n`2026-05-14-handoff-lifecycle-and-kill-ux.md` §C/§F). Emitted by the\n`POST /api/units/{unit}/handoff-and-restart` endpoint on every step;\nstreamed to SSE consumers as event kind `handoff_ritual`."
          }
        ],
        "description": "Events emitted by the core when state changes occur.\n\nConsumers call [`TmaiCore::subscribe()`] to receive these events\nvia a `broadcast::Receiver`.\n\nThis enum is the single source of truth for SSE event shapes —\nthe TypeScript discriminated union in\n`crates/tmai-app/web/src/types/generated/CoreEvent.ts` is generated\nfrom this definition by ts-rs (#446). Do not edit that file by hand."
      },
      "CountsWire": {
        "type": "object",
        "description": "Bucket counts for one repo's `Settled` section — mirrors\n`compose::Counts`.",
        "required": [
          "total",
          "in_play",
          "warm",
          "cold",
          "foundations",
          "superseded",
          "stale_suspect"
        ],
        "properties": {
          "cold": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "foundations": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "in_play": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "stale_suspect": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "superseded": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "total": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "warm": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          }
        }
      },
      "CurrencyItemWire": {
        "type": "object",
        "description": "One row of the `⚠ Currency sweep` callout.",
        "required": [
          "slug",
          "title",
          "stale",
          "last_verified",
          "remedy"
        ],
        "properties": {
          "last_verified": {
            "type": "string"
          },
          "remedy": {
            "type": "string"
          },
          "slug": {
            "type": "string"
          },
          "stale": {
            "$ref": "#/components/schemas/StaleSinceWire"
          },
          "title": {
            "type": "string"
          }
        }
      },
      "DecisionCategory": {
        "type": "string",
        "description": "What kind of decision this is — drives the workbench's temperature treatment.\n\n`category` is the *kind* (blast radius / abstraction), not the authority.\nAuthority is not a stored field: it is derived from the slot\n(`doc/decisions/` ⇒ a human-accepted decision vs `doc/approaches/` ⇒ a\nProducer-run experiment), `category: foundational`, and the act itself\n(accepting a decision is a human gate; running an approach is the\nProducer's). See `doc/decisions/2026-05-16-authority-attaches-to-the-act.md`.",
        "enum": [
          "scoped",
          "principle",
          "foundational"
        ]
      },
      "DecisionStatus": {
        "type": "string",
        "description": "Current standing of a decision.",
        "enum": [
          "draft",
          "proposed",
          "accepted",
          "superseded",
          "superseded-in-part"
        ]
      },
      "DecisionWire": {
        "type": "object",
        "description": "One decision projected to the wire — the human-side complement of the\ninternal `compose::DecisionView`. `last_verified` is rendered as the\nISO-8601 date the frontmatter carries (e.g. `\"2026-05-14\"`).",
        "required": [
          "slug",
          "title",
          "status",
          "category",
          "governs",
          "last_verified",
          "contract_surface",
          "superseded_by",
          "strengthened_by",
          "excerpt"
        ],
        "properties": {
          "category": {
            "$ref": "#/components/schemas/DecisionCategory"
          },
          "contract_surface": {
            "type": "boolean"
          },
          "excerpt": {
            "type": "string",
            "description": "For 🔴 in-play: the `## Decision` section of the body. For\neverything else: a one-paragraph summary."
          },
          "governs": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "last_verified": {
            "type": "string"
          },
          "pending_projection": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/PendingProjectionWire",
                "description": "`Some` ONLY when this decision is \"accepted (projection: pending)\" — a\nrelay-gated accept-commit for its slug exists in the core-only δ branch\nwhile `status` here (read from main) still says `proposed`. **Derived,\nnever stored** (relay-channel B3.3-a: no hidden second state — the δ\ncommit's existence IS the accept). Absent (skipped from JSON) otherwise,\nso the wire stays lockstep-free and pre-P3 clients are unaffected.\n\nP2 ships the field (wire readiness) but never populates it: the\nderivation is **P3's** (B3 variant / projection) and is *not* an accept\ngate. P2 keeps it `None`."
              }
            ]
          },
          "slug": {
            "type": "string"
          },
          "stale_since": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/StaleSinceWire",
                "description": "`Some` when a `governs:` path changed in git after `last_verified` —\nthe workbench's drift signal."
              }
            ]
          },
          "status": {
            "$ref": "#/components/schemas/DecisionStatus"
          },
          "strengthened_by": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "superseded_by": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "title": {
            "type": "string"
          }
        }
      },
      "DecisionsResponse": {
        "type": "object",
        "description": "`GET /api/units/{unit}/decisions` response — the `Settled` section of\nthe unit's hand-over, per repo, JSON-projected. Composed on demand\n(no cache), so the client polls when it wants fresh data.\n\nFor a single-repo unit this collapses to `repos.len() == 1`. For a\nmulti-repo unit (per\n`doc/decisions/2026-05-14-producer-capability-valve-principle.md`\n§C/§D) each repo is composed independently; the React side renders a\ngroup per repo without any cross-repo aggregation.",
        "required": [
          "unit",
          "composed_at",
          "repos"
        ],
        "properties": {
          "composed_at": {
            "type": "string",
            "description": "RFC3339 compose timestamp (the moment the server gathered + composed)."
          },
          "repos": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RepoDecisionsWire"
            }
          },
          "unit": {
            "type": "string"
          }
        }
      },
      "DetectionSource": {
        "type": "string",
        "description": "Source of agent state detection\n\nWire format is `snake_case` to match the contract-layer enums\n(`VendorAvailabilityState`, `CapacityCauseSummary`, `BundleStatus`). The\nPascalCase names (e.g. `\"HttpHook\"`) shipped prior to #7 are still\naccepted on deserialization via `#[serde(alias = ...)]` so that\npreviously-persisted `MonitoredAgent.detection_source` payloads continue\nto round-trip.",
        "enum": [
          "http_hook",
          "ipc_socket",
          "web_socket",
          "capture_pane"
        ]
      },
      "DiffSummarySnapshot": {
        "type": "object",
        "description": "Diff statistics snapshot for API consumers",
        "required": [
          "files_changed",
          "insertions",
          "deletions"
        ],
        "properties": {
          "deletions": {
            "type": "integer",
            "description": "Number of lines deleted",
            "minimum": 0
          },
          "files_changed": {
            "type": "integer",
            "description": "Number of files changed",
            "minimum": 0
          },
          "insertions": {
            "type": "integer",
            "description": "Number of lines inserted",
            "minimum": 0
          }
        }
      },
      "DispatchId": {
        "type": "string",
        "description": "Globally unique identifier for a dispatched unit of work.\n\nBacked by UUID v7 so that records sort lexicographically by creation\ntime without needing a separate timestamp index. Serialized as a plain\nstring (`#[serde(transparent)]`) so external consumers can treat the\nfield as an opaque token without learning the wrapper type."
      },
      "DispatchIntentSummary": {
        "type": "object",
        "description": "Redacted summary of a dispatch intent attached to\n[`CoreEvent::DispatchRejected`] and other contract-layer events.\n\nThe full prompt and any credentials **must never** appear here — callers\nthat need the raw intent go through an out-of-band audit channel\n(issue #463, open question 2). Consumers seeing `prompt_hash` can\ncorrelate back to the original dispatch request without learning its\ncontents.",
        "required": [
          "project_path",
          "role"
        ],
        "properties": {
          "bundle_id": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/BundleId",
                "description": "Bundle id when this dispatch is part of a multi-project bundle."
              }
            ]
          },
          "issue_number": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Associated GitHub issue number, if any.",
            "minimum": 0
          },
          "pr_number": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Associated pull request number, if any.",
            "minimum": 0
          },
          "project_path": {
            "type": "string",
            "description": "Absolute path to the target project / worktree root."
          },
          "prompt_hash": {
            "type": [
              "string",
              "null"
            ],
            "description": "Hex digest (implementation-chosen hash, e.g. SHA-256) of the\ndispatched prompt. Lets auditors correlate this event with the\nrecorded intent without exposing the prompt text."
          },
          "role": {
            "type": "string",
            "description": "Agent role requested (e.g. \"implementer\", \"reviewer\")."
          }
        }
      },
      "DispatchKind": {
        "oneOf": [
          {
            "type": "object",
            "description": "Work attached to a GitHub issue (no PR yet).",
            "required": [
              "number",
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "issue"
                ]
              },
              "number": {
                "type": "integer",
                "format": "int64",
                "description": "Issue number.",
                "minimum": 0
              }
            }
          },
          {
            "type": "object",
            "description": "Work attached to an existing pull request (e.g. follow-up commits).",
            "required": [
              "number",
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "pr"
                ]
              },
              "number": {
                "type": "integer",
                "format": "int64",
                "description": "PR number.",
                "minimum": 0
              }
            }
          },
          {
            "type": "object",
            "description": "Free-form work not tied to an issue or PR.",
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "standalone"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A reviewer dispatched against an existing PR.",
            "required": [
              "pr",
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "review"
                ]
              },
              "pr": {
                "type": "integer",
                "format": "int64",
                "description": "PR being reviewed.",
                "minimum": 0
              }
            }
          },
          {
            "type": "object",
            "description": "An orchestrator dispatch.",
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "orchestrator"
                ]
              }
            }
          }
        ],
        "description": "Classification of what was dispatched. Drives notification phrasing,\ngatekeeper rules (#11), and selector disambiguation (e.g. an\nimplementer worktree and a review worktree both reference the same PR\nnumber — `kind` separates them)."
      },
      "DispatchRefs": {
        "type": "object",
        "description": "Stable references attached to a dispatch at request time. These are\nthe fields a notification, audit log, or external observer should read\n— not the live agent snapshot, which may be stale.",
        "required": [
          "project_root",
          "title"
        ],
        "properties": {
          "base_branch": {
            "type": [
              "string",
              "null"
            ],
            "description": "Branch the worktree was forked from."
          },
          "branch": {
            "type": [
              "string",
              "null"
            ],
            "description": "Branch the dispatched work targets."
          },
          "issue_number": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Issue number when applicable.",
            "minimum": 0
          },
          "pr_number": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "PR number when applicable.",
            "minimum": 0
          },
          "project_root": {
            "type": "string",
            "description": "Absolute path to the project the dispatch targets. Resolver scoping\nuses this to prevent the cross-project id collisions described in\nthe issue."
          },
          "title": {
            "type": "string",
            "description": "Human-readable title chosen at dispatch time so notifications\nalways have something to render even before the agent prints\nanything."
          },
          "worktree_name": {
            "type": [
              "string",
              "null"
            ],
            "description": "Worktree name when the dispatch creates or attaches to a worktree."
          }
        }
      },
      "DispatchSnapshot": {
        "type": "object",
        "description": "UI-facing snapshot of a dispatched unit of work.",
        "required": [
          "id",
          "kind",
          "refs",
          "state",
          "role",
          "spawner",
          "spawned_at",
          "title",
          "display_subtitle",
          "is_terminal"
        ],
        "properties": {
          "agent_target": {
            "type": [
              "string",
              "null"
            ],
            "description": "Live agent tmux target (e.g. \"main:0.1\"), if the agent has been attached."
          },
          "bundle_id": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/BundleId",
                "description": "Bundle id when this dispatch is part of a multi-repo bundle."
              }
            ]
          },
          "display_subtitle": {
            "type": "string",
            "description": "Short subtitle derived from dispatch kind (e.g. \"Issue #42\")."
          },
          "duration_ms": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Elapsed milliseconds from spawn to terminal state, if finished.",
            "minimum": 0
          },
          "finished_at": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time",
            "description": "Wall-clock time when the dispatch reached a terminal state, if any."
          },
          "id": {
            "$ref": "#/components/schemas/DispatchId",
            "description": "Durable dispatch id (UUID v7)."
          },
          "is_terminal": {
            "type": "boolean",
            "description": "Whether the dispatch has reached a terminal state (Stopped / Failed)."
          },
          "kind": {
            "$ref": "#/components/schemas/DispatchKind",
            "description": "What kind of work was dispatched."
          },
          "pty_session_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "PTY session id, if the agent was spawned via the PTY layer."
          },
          "refs": {
            "$ref": "#/components/schemas/DispatchRefs",
            "description": "Stable references captured at request time."
          },
          "role": {
            "$ref": "#/components/schemas/SpawnRole",
            "description": "Agent role this dispatch was filled with."
          },
          "spawned_at": {
            "type": "string",
            "format": "date-time",
            "description": "Wall-clock creation time."
          },
          "spawner": {
            "$ref": "#/components/schemas/SpawnerHandle",
            "description": "Who or what originated this dispatch."
          },
          "state": {
            "$ref": "#/components/schemas/DispatchState",
            "description": "Current lifecycle state."
          },
          "title": {
            "type": "string",
            "description": "Human-readable title chosen at dispatch time."
          }
        }
      },
      "DispatchState": {
        "oneOf": [
          {
            "type": "object",
            "description": "Created, not yet handed to a spawner.",
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "pending"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Spawner is running but no agent process has been observed yet.",
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "spawning"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Live agent attached and producing output.",
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "working"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Live agent attached and idle (no in-flight tool call).",
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "idle"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Paused waiting for a human or external signal.",
            "required": [
              "reason",
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "blocked"
                ]
              },
              "reason": {
                "type": "string",
                "description": "Free-text reason for the block."
              }
            }
          },
          {
            "type": "object",
            "description": "Terminated normally.",
            "required": [
              "reason",
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "stopped"
                ]
              },
              "reason": {
                "type": "string",
                "description": "Free-text reason (e.g. \"agent_exited\", \"killed_by_user\")."
              }
            }
          },
          {
            "type": "object",
            "description": "Terminated abnormally.",
            "required": [
              "error",
              "kind"
            ],
            "properties": {
              "error": {
                "type": "string",
                "description": "Free-text error description."
              },
              "kind": {
                "type": "string",
                "enum": [
                  "failed"
                ]
              }
            }
          }
        ],
        "description": "Lifecycle position of a dispatch.\n\nPersisted alongside the dispatch record so a tmai restart can restore\nthe ledger without consulting any live process state."
      },
      "EntityChange": {
        "type": "string",
        "description": "Whether an entity was upserted (created or updated) or removed.\n\nUsed in Entity-Update [`CoreEvent`] variants and [`crate::api::envelope::EntityUpdateEnvelope`].",
        "enum": [
          "Upserted",
          "Removed"
        ]
      },
      "ErrorCode": {
        "type": "string",
        "description": "Machine-readable error code — stable across releases.\n\nAdded codes **never** get removed: deprecations are handled with serde\naliases so old payloads still deserialize. Callers switch on this value\ninstead of `message` text.",
        "enum": [
          "CapacityExceeded",
          "VendorUnavailable",
          "QueueFull",
          "AgentNotFound",
          "AgentInTerminalState",
          "WorktreeConflict",
          "AlreadyRunning",
          "PermissionDenied",
          "TokenInvalid",
          "InvalidArgument",
          "SchemaMismatch",
          "VendorError",
          "TmuxError",
          "IpcError",
          "Internal"
        ]
      },
      "FoundationalDueWire": {
        "type": "object",
        "description": "One row of the `⚠ Trajectory check due` callout — a foundational\ndecision whose `last_verified` is older than\n`workbench::FOUNDATIONAL_DUE_DAYS`.",
        "required": [
          "slug",
          "title",
          "last_verified",
          "age_days",
          "remedy"
        ],
        "properties": {
          "age_days": {
            "type": "integer",
            "format": "int64"
          },
          "last_verified": {
            "type": "string"
          },
          "remedy": {
            "type": "string"
          },
          "slug": {
            "type": "string"
          },
          "title": {
            "type": "string"
          }
        }
      },
      "HandoffRitualEvent": {
        "allOf": [
          {
            "$ref": "#/components/schemas/HandoffRitualPhase",
            "description": "Phase reached by this event (carries `reason` on `Escalate`)."
          },
          {
            "type": "object",
            "required": [
              "unit",
              "ritual_id"
            ],
            "properties": {
              "message": {
                "type": [
                  "string",
                  "null"
                ],
                "description": "Free-form human-readable context (omitted when absent on the wire)."
              },
              "new_agent_id": {
                "type": [
                  "string",
                  "null"
                ],
                "description": "Canonical AgentId of the newly spawned Producer. Set only on `Ready`."
              },
              "ritual_id": {
                "type": "string",
                "description": "Fresh UUID assigned when the ritual started."
              },
              "unit": {
                "type": "string",
                "description": "Unit name the ritual is being run for."
              }
            }
          }
        ],
        "description": "SSE payload emitted at each transition of the\n`POST /api/units/{unit}/handoff-and-restart` ritual.\n\nWired into [`CoreEvent::HandoffRitual`]; surfaces on `/api/events` as event\nkind `handoff_ritual`. `ritual_id` is a fresh UUID minted at ritual start\nso concurrent rituals across units (or operator retries) don't alias."
      },
      "HandoffRitualPhase": {
        "oneOf": [
          {
            "type": "object",
            "description": "Ritual prompt was delivered to the active Producer (§C step 2).",
            "required": [
              "phase"
            ],
            "properties": {
              "phase": {
                "type": "string",
                "enum": [
                  "prompted"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "`active.md` baton landed fresh + validated (§C steps 3-4).",
            "required": [
              "phase"
            ],
            "properties": {
              "phase": {
                "type": "string",
                "enum": [
                  "validated"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Old Producer was killed via PTY-server `kill_agent` (§C step 5).",
            "required": [
              "phase"
            ],
            "properties": {
              "phase": {
                "type": "string",
                "enum": [
                  "killed"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "New Producer spawn submitted (§C step 6); not yet `started`.",
            "required": [
              "phase"
            ],
            "properties": {
              "phase": {
                "type": "string",
                "enum": [
                  "launching"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "New Producer reported `attention = started`; ritual complete (§C step 7).",
            "required": [
              "phase"
            ],
            "properties": {
              "phase": {
                "type": "string",
                "enum": [
                  "ready"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Failure path; `reason` is a machine-readable code per DR §D failure\ninventory (`no_active_producer`, `multiple_producers`,\n`prompt_send_failed`, `timeout`, `malformed_handoff`, `kill_failed`,\n`spawn_failed`). Consumers route every variant to the same operator\ndialog (§E).",
            "required": [
              "reason",
              "phase"
            ],
            "properties": {
              "phase": {
                "type": "string",
                "enum": [
                  "escalate"
                ]
              },
              "reason": {
                "type": "string",
                "description": "Machine-readable failure reason (see variant doc)."
              }
            }
          }
        ],
        "description": "Phase of the Producer handoff-and-restart ritual (DR\n`2026-05-14-handoff-lifecycle-and-kill-ux.md` §C).\n\nPhases progress strictly in the order `Prompted → Validated → Killed →\nLaunching → Ready`; any step can short-circuit to `Escalate` with a\nmachine-readable `reason` (§D — uniform tier-1 escalation, no per-mode\npolicy). Consumers driving the WebUI overlay (`§E`) treat all `Escalate`\nvariants as the terminal failure state regardless of reason."
      },
      "Milestone": {
        "type": "object",
        "description": "A single milestone event in the task lifecycle.\n\nPart of the type-sharing PoC (#446): generated TypeScript lives in\n`crates/tmai-app/web/src/types/generated/Milestone.ts`.",
        "required": [
          "at",
          "event"
        ],
        "properties": {
          "at": {
            "type": "string",
            "format": "date-time",
            "description": "When the milestone occurred"
          },
          "event": {
            "type": "string",
            "description": "Human-readable event description"
          }
        }
      },
      "Outcome": {
        "oneOf": [
          {
            "type": "object",
            "description": "A `Revert \"<title>\"` commit later touched a path this verdict bore on\n— the absorbed change was wrong. Strongest false-negative signal.",
            "required": [
              "commit_sha",
              "date",
              "kind"
            ],
            "properties": {
              "commit_sha": {
                "type": "string"
              },
              "date": {
                "type": "string",
                "format": "date"
              },
              "kind": {
                "type": "string",
                "enum": [
                  "revert_commit"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A `fix:` commit shortly after touched a path this verdict bore on —\nthe absorbed change introduced a defect. Weaker signal (fixes happen\nfor many reasons), but still informative in aggregate.",
            "required": [
              "commit_sha",
              "date",
              "kind"
            ],
            "properties": {
              "commit_sha": {
                "type": "string"
              },
              "date": {
                "type": "string",
                "format": "date"
              },
              "kind": {
                "type": "string",
                "enum": [
                  "hotfix_commit"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "CI failed on a PR shortly after, and a follow-up PR fixed it — the\nabsorbed change broke the build.",
            "required": [
              "failing_pr",
              "fix_pr",
              "kind"
            ],
            "properties": {
              "failing_pr": {
                "type": "integer",
                "format": "int32",
                "minimum": 0
              },
              "fix_pr": {
                "type": "integer",
                "format": "int32",
                "minimum": 0
              },
              "kind": {
                "type": "string",
                "enum": [
                  "ci_fail_fix"
                ]
              }
            }
          }
        ],
        "description": "An *objective* outcome observed after a triage verdict, used as ground\ntruth for calibration. Per DR §B.2: the human's accept/reject is **not**\nhere — only failure signals the world produces independently.\n\nThe data is structurally complete in stage 1; the *detection* (scanning\ngit log / PR history to populate these) lands in stage 5."
      },
      "PendingProjectionWire": {
        "type": "object",
        "description": "Evidence that an accepted decision's projection is still pending: the\ncore-only δ branch carrying the attributable accept-commit, and that\ncommit's SHA. The pair is what P3 derives \"accepted (projection: pending)\"\nfrom (a δ accept-commit exists while main still reads `proposed`); it carries\nno stored state of its own.",
        "required": [
          "accept_branch",
          "accept_commit"
        ],
        "properties": {
          "accept_branch": {
            "type": "string",
            "description": "The δ branch on which the accept-commit lives (e.g. `tmai-accept`)."
          },
          "accept_commit": {
            "type": "string",
            "description": "Full SHA of the δ accept-commit whose existence IS the durable accept."
          }
        }
      },
      "PermissionMode": {
        "type": "string",
        "description": "Permission mode reported by Claude Code in hook payloads.\n\nMaps to the session's current permission level (e.g., \"default\", \"plan\",\n\"dontAsk\", \"acceptEdits\").",
        "enum": [
          "default",
          "plan",
          "dontAsk",
          "acceptEdits",
          "auto"
        ]
      },
      "PrDiffResponse": {
        "type": "object",
        "description": "`GET /api/github/pr/diff` response — a PR's raw unified patch\n(`gh pr diff <n> --patch`). Deliberately verbatim text, not a parsed\nhunk AST: the diff *is* the contract; rendering structure is the React\nviewer's concern (DR §A — review the code diff in-tmai).",
        "required": [
          "repo",
          "pr_number",
          "patch"
        ],
        "properties": {
          "patch": {
            "type": "string",
            "description": "Verbatim unified-diff text. Empty string is a valid response (a PR\nwhose head equals its base produces an empty patch)."
          },
          "pr_number": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "repo": {
            "type": "string",
            "description": "Absolute repo path the diff was read from (echoes the request)."
          }
        }
      },
      "PrSummaryWire": {
        "type": "object",
        "description": "One PR projected to the wire. Field-for-field parity with\n[`crate::github::PrInfo`] (the single-repo `GET /api/github/prs` shape)\nso the React client renders a cross-repo entry with the exact same\ncomponent it already uses for the per-repo list.\n\n`review_decision` / `check_status` are carried as the raw uppercased\nstrings `gh` emits (`\"APPROVED\"`, `\"CHANGES_REQUESTED\"`, `\"SUCCESS\"`,\n…) rather than typed enums. WHY: the domain enums\n(`github::ReviewDecision` / `CheckStatus`) do not derive `ts_rs::TS` /\n`utoipa::ToSchema`, and giving them those derives would cascade new\ngenerated TS enums whose casing must then be kept in lock-step with\n`gh`'s `SCREAMING_SNAKE_CASE` serde repr — a downstream `tsc -b` break\nrisk for zero UI gain (the viewer only displays the badge text). A\nstring passthrough keeps the value byte-identical to what\n`GET /api/github/prs` already serializes; the conversion below derives\nit from the very same `serde` impl, so the two endpoints can never drift.",
        "required": [
          "number",
          "title",
          "state",
          "head_branch",
          "head_sha",
          "base_branch",
          "url",
          "is_draft",
          "additions",
          "deletions",
          "comments",
          "reviews",
          "author"
        ],
        "properties": {
          "additions": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "author": {
            "type": "string"
          },
          "base_branch": {
            "type": "string"
          },
          "check_status": {
            "type": [
              "string",
              "null"
            ],
            "description": "Raw rolled-up check status (`\"SUCCESS\"` / `\"FAILURE\"` / `\"PENDING\"`),\nor `null` when there are no checks."
          },
          "ci_local_attestation": {
            "type": [
              "string",
              "null"
            ],
            "description": "The ci-local attestation the Producer stored alongside the delivered\nmarker, if any (approach `2026-05-26-producer-supplied-override-\nattestation.md`, O2-d). `Some` only when `producer_reviewed` is true\nAND the Producer supplied an attestation via `mark_pr_reviewed`.\n\nNeutral evidence-of-verification, **not** an approval — same §E\nboundary as `producer_reviewed`. The react half (Phase B) reads it to\npre-fill the billing-dead override textarea so the operator need not\nhand-paste the ci-local summary; absence simply means the operator\nfalls back to the manual paste (the O2-a path). The backend consumes\nthe *stored* copy directly — this wire field is for the UI, not the\nauthoritative override input.\n\nOptional / serde-default with the same lockstep-free discipline as\n`producer_reviewed`: absent on the wire when `None`, and a payload\nomitting it deserializes to `None`."
          },
          "comments": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "deletions": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "head_branch": {
            "type": "string"
          },
          "head_sha": {
            "type": "string"
          },
          "is_draft": {
            "type": "boolean"
          },
          "merge_commit_sha": {
            "type": [
              "string",
              "null"
            ],
            "description": "Set only for merged PRs (the unit list is open-only, so this is\n`null` in practice — kept for shape parity with `PrInfo`)."
          },
          "number": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "producer_reviewed": {
            "type": "boolean",
            "description": "`true` when the Producer has **delivered** its contract-anchored\nΔ-brief for this PR — the soft-valve unlock condition of dev-loop\nDR `2026-05-16-dev-loop-completes-in-tmai` §E (approach\n`2026-05-17-producer-review-gated-in-tmai-merge`).\n\n**Delivered, NOT approved.** A neutral delivered-marker: it states\nonly that the brief reached the operator, never that the Producer\napproves/blocks the merge or judges it merge-worthy (§E load-bearing\nboundary — the Producer's opinion is *content of the brief*, never\nthis bit; the merge decision stays wholly the operator's). The react\nhalf consumes this to make the reviewed merge path frictionless\n(dialog / colour / dismissible override) — that button state machine\nis a separate dispatch; core only exposes the bit, it never blocks a\nmerge on it. Not a `PrInfo` field: it is overlaid from the transient\n[`AppState::producer_reviewed_prs`](crate::state::AppState) in\n[`build_unit_prs_response`], so it resets on engine restart (the\nProducer re-briefs).\n\n`default` + `skip_serializing_if` keep it optional with the exact\n#404 `is_producer` discipline: the field is absent on the wire when\n`false`, and a payload omitting it deserializes to `false` — a\nclient omitting it stays lockstep-free."
          },
          "review_decision": {
            "type": [
              "string",
              "null"
            ],
            "description": "Raw `gh` review-decision string (`\"APPROVED\"` / `\"CHANGES_REQUESTED\"`\n/ `\"REVIEW_REQUIRED\"`), or `null` when GitHub reports none."
          },
          "reviews": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "state": {
            "type": "string"
          },
          "title": {
            "type": "string"
          },
          "url": {
            "type": "string"
          }
        }
      },
      "QueueAgentEntry": {
        "type": "object",
        "description": "Per-agent view of the prompt queue.",
        "required": [
          "agent_id",
          "agent_stable_id",
          "agent_display_label",
          "queue",
          "total_count"
        ],
        "properties": {
          "agent_display_label": {
            "type": "string",
            "description": "Best display label for the agent at snapshot time"
          },
          "agent_id": {
            "type": "string",
            "description": "Canonical agent identifier (`<scheme>:<id>`, matches `AgentSnapshot.id`)"
          },
          "agent_stable_id": {
            "type": "string",
            "description": "tmux pane target (e.g. \"main:0.1\") — the key used in `AppState.prompt_queue`"
          },
          "oldest_queued_at": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time",
            "description": "When the oldest prompt was enqueued"
          },
          "queue": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/QueuedPrompt"
            },
            "description": "Ordered list of queued prompts (oldest first)"
          },
          "total_count": {
            "type": "integer",
            "format": "int32",
            "description": "Total number of queued prompts (== queue.len())",
            "minimum": 0
          }
        }
      },
      "QueueSnapshot": {
        "type": "object",
        "description": "Snapshot of the global prompt queue, one entry per agent that has\nat least one queued prompt.",
        "required": [
          "entries"
        ],
        "properties": {
          "entries": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/QueueAgentEntry"
            },
            "description": "One entry per agent that currently has queued prompts (oldest-first within each agent)"
          }
        }
      },
      "QueuedPrompt": {
        "type": "object",
        "description": "Owned snapshot of a `MonitoredAgent`, returned by query methods.\n\nAll string fields are cloned out of the locked state so that callers\nA prompt entry held in an agent's pending queue",
        "required": [
          "id",
          "prompt",
          "queued_at"
        ],
        "properties": {
          "id": {
            "type": "string",
            "description": "UUID v7 assigned at enqueue time (time-ordered, stable cancel key)"
          },
          "origin": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ActionOrigin",
                "description": "Origin of the enqueue request"
              }
            ]
          },
          "prompt": {
            "type": "string",
            "description": "The prompt text to be delivered when the agent becomes idle"
          },
          "queued_at": {
            "type": "string",
            "format": "date-time",
            "description": "When this prompt was enqueued (RFC 3339)"
          }
        }
      },
      "RepoApproachesWire": {
        "type": "object",
        "description": "One repo's projected `▣ Active approaches` slice. The list carries\n`status: active` records only; validated / rejected / replaced records\nare filtered out at compose time (preserved in memory as audit-trail\ncontext, not surfaced on the wire yet).",
        "required": [
          "repo_label",
          "repo_root",
          "primary",
          "active"
        ],
        "properties": {
          "active": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ApproachWire"
            }
          },
          "primary": {
            "type": "boolean"
          },
          "repo_head": {
            "type": [
              "string",
              "null"
            ]
          },
          "repo_label": {
            "type": "string"
          },
          "repo_root": {
            "type": "string"
          }
        }
      },
      "RepoDecisionsWire": {
        "type": "object",
        "description": "One repo's projected `Settled` section. The bucketed lists are\nalready temperature-sorted; the renderer (React) iterates in order.",
        "required": [
          "repo_label",
          "repo_root",
          "primary",
          "counts",
          "currency_sweep",
          "foundational_due",
          "foundations",
          "in_play",
          "warm",
          "cold",
          "superseded"
        ],
        "properties": {
          "cold": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DecisionWire"
            }
          },
          "counts": {
            "$ref": "#/components/schemas/CountsWire"
          },
          "currency_sweep": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CurrencyItemWire"
            }
          },
          "foundational_due": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FoundationalDueWire"
            }
          },
          "foundations": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DecisionWire"
            }
          },
          "in_play": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DecisionWire"
            }
          },
          "primary": {
            "type": "boolean"
          },
          "repo_head": {
            "type": [
              "string",
              "null"
            ],
            "description": "Short HEAD sha of `repo_root`, or `None` when it is not a git repo."
          },
          "repo_label": {
            "type": "string",
            "description": "Path leaf of the repo root, e.g. `\"tmai-core\"`. Drives the group\nheader in the React renderer."
          },
          "repo_root": {
            "type": "string",
            "description": "Absolute repo root as a string (filesystem path). Surfaced for\ndisambiguation and click-through; not parsed by the client."
          },
          "superseded": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DecisionWire"
            }
          },
          "warm": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DecisionWire"
            }
          }
        }
      },
      "RepoPrsWire": {
        "type": "object",
        "description": "One repo's slice of the unit-scoped PR list. `repo_label` is the stable\ndirectory basename (e.g. `\"tmai-core\"`) — the same scheme\n[`crate::config::ResolvedUnit::from_dir`] uses for unit names; `repo_path`\nis the absolute path the diff/merge endpoints take as their `repo` param.",
        "required": [
          "repo_path",
          "repo_label",
          "primary",
          "prs"
        ],
        "properties": {
          "billing_dead": {
            "type": "boolean",
            "description": "`true` when `[github.<repo>] billing_dead = true` is set for this\nrepo (`Settings::repo_billing_dead`). Surfaced so the react UI can\ndecide *where* to show the billing-dead CI-gate override affordance\n(approach `2026-05-20-billing-dead-ci-safe-override`, O1-b) — the\naffordance belongs only on a repo the operator has flagged. This is\na read-only projection of operator config; the backend\n(`validate_billing_dead_override`) is the enforcing gate, never this\nbit. Overlaid in [`build_unit_prs_response`] from an injected\npredicate, so the function stays pure / IO-free.\n\n`default` + `skip_serializing_if` keep it optional with the exact\n#404 `is_producer` discipline: absent on the wire when `false`, and a\npayload omitting it deserializes to `false` — clients stay lockstep-free."
          },
          "primary": {
            "type": "boolean",
            "description": "`true` for the unit's primary repo (where the Producer is launched)."
          },
          "prs": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PrSummaryWire"
            },
            "description": "Open PRs in this repo, newest first (descending PR number)."
          },
          "repo_label": {
            "type": "string"
          },
          "repo_path": {
            "type": "string"
          }
        }
      },
      "RetryHint": {
        "oneOf": [
          {
            "type": "object",
            "description": "Retry after a specific wall-clock instant (RFC3339).\n\nUsed when the underlying resource knows when availability resumes\n(e.g. vendor rate-limit reset).",
            "required": [
              "resume_at",
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "retry_after"
                ]
              },
              "resume_at": {
                "type": "string",
                "format": "date-time",
                "description": "ISO-8601 / RFC3339 timestamp at which the caller may retry."
              }
            }
          },
          {
            "type": "object",
            "description": "Retry after a millisecond backoff (caller-driven clock).",
            "required": [
              "ms",
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "backoff_ms"
                ]
              },
              "ms": {
                "type": "integer",
                "format": "int64",
                "description": "Milliseconds to wait before retrying.",
                "minimum": 0
              }
            }
          },
          {
            "type": "object",
            "description": "The failure is not retryable; the caller should surface it instead.",
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "not_retryable"
                ]
              }
            }
          }
        ],
        "description": "Advisory retry guidance attached to a [`TmaiError`].\n\nThe hint is informational only — the caller decides whether to act on it.\nRetry orchestration is explicitly out of scope for v1 of the taxonomy."
      },
      "ReviewExtensionWire": {
        "type": "object",
        "description": "One entry in an approach's extend ledger, wire-projected.",
        "required": [
          "extended_on",
          "new_trigger",
          "reason"
        ],
        "properties": {
          "extended_on": {
            "type": "string",
            "description": "ISO-8601 date the extension was recorded."
          },
          "new_trigger": {
            "$ref": "#/components/schemas/ReviewTriggerWire"
          },
          "reason": {
            "type": "string"
          }
        }
      },
      "ReviewTriggerWire": {
        "oneOf": [
          {
            "type": "object",
            "description": "Calendar fallback — `value` is `YYYY-MM-DD`.",
            "required": [
              "value",
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "date"
                ]
              },
              "value": {
                "type": "string"
              }
            }
          },
          {
            "type": "object",
            "description": "`reference` is `<owner>/<repo>#<number>`.",
            "required": [
              "ref",
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "pr-closed"
                ]
              },
              "ref": {
                "type": "string"
              }
            }
          },
          {
            "type": "object",
            "description": "`reference` is `<owner>/<repo>#<number>`.",
            "required": [
              "ref",
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "pr-merged"
                ]
              },
              "ref": {
                "type": "string"
              }
            }
          },
          {
            "type": "object",
            "description": "`reference` is `<owner>/<repo>#<number>`.",
            "required": [
              "ref",
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "issue-closed"
                ]
              },
              "ref": {
                "type": "string"
              }
            }
          },
          {
            "type": "object",
            "description": "`reference` is a decision-record slug; `target_status` matches the\n`DecisionStatus` wire enum (`accepted` / `proposed` / etc.).",
            "required": [
              "ref",
              "target-status",
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "decision-status"
                ]
              },
              "ref": {
                "type": "string"
              },
              "target-status": {
                "type": "string"
              }
            }
          },
          {
            "type": "object",
            "description": "`reference` is an approach-record slug; `target_status` matches the\n`ApproachStatus` wire enum (`validated` / `rejected` / etc.).",
            "required": [
              "ref",
              "target-status",
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "approach-status"
                ]
              },
              "ref": {
                "type": "string"
              },
              "target-status": {
                "type": "string"
              }
            }
          },
          {
            "type": "object",
            "description": "Never auto-fires. `description` records the operator's intent for a\nfuture Producer suggestion pass.",
            "required": [
              "description",
              "kind"
            ],
            "properties": {
              "description": {
                "type": "string"
              },
              "kind": {
                "type": "string",
                "enum": [
                  "manual"
                ]
              }
            }
          }
        ],
        "description": "One review trigger, wire-projected. Tagged on `kind` so the TS shape\nmatches the Rust [`crate::workbench::ReviewTrigger`] one-for-one;\n`Date.value` becomes an ISO-8601 string on the wire (the underlying\n`NaiveDate` is private to the parser)."
      },
      "RuntimeSnapshot": {
        "type": "object",
        "description": "UI-facing snapshot of runtime infrastructure state.",
        "required": [
          "capacity",
          "vendors"
        ],
        "properties": {
          "capacity": {
            "$ref": "#/components/schemas/CapacityInfo",
            "description": "Current and configured agent capacity."
          },
          "vendors": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/VendorInfo"
            },
            "description": "Availability state of each tracked AI vendor."
          }
        }
      },
      "SpawnRole": {
        "type": "string",
        "description": "Role of a spawned agent.",
        "enum": [
          "orchestrator",
          "implementer",
          "reviewer",
          "manual"
        ]
      },
      "SpawnerHandle": {
        "oneOf": [
          {
            "type": "object",
            "description": "Spawned by another dispatch (e.g. an orchestrator delegating).",
            "required": [
              "id",
              "kind"
            ],
            "properties": {
              "id": {
                "$ref": "#/components/schemas/DispatchId",
                "description": "The parent dispatch's id."
              },
              "kind": {
                "type": "string",
                "enum": [
                  "dispatch"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Spawned directly by a human via a UI surface.",
            "required": [
              "interface",
              "kind"
            ],
            "properties": {
              "interface": {
                "type": "string",
                "description": "Which interface: \"webui\", \"tui\", \"mcp\", \"cli\", …"
              },
              "kind": {
                "type": "string",
                "enum": [
                  "human"
                ]
              }
            }
          }
        ],
        "description": "Who or what spawned a dispatch. For orchestrator hierarchies (#9) this\nchain is what lets a downstream observer attribute work back to its\noriginating human or parent dispatch."
      },
      "StaleSinceWire": {
        "type": "object",
        "description": "The drift evidence for one decision: which `governs:` path moved and\nthe commit that moved it.",
        "required": [
          "path",
          "change_date",
          "change_sha",
          "change_subject"
        ],
        "properties": {
          "change_date": {
            "type": "string"
          },
          "change_sha": {
            "type": "string"
          },
          "change_subject": {
            "type": "string"
          },
          "path": {
            "type": "string"
          }
        }
      },
      "TaskMetaSnapshot": {
        "type": "object",
        "description": "A single task entry merging git worktree info with .task-meta/ metadata.\n\nPreviously named `TaskMetaEntry`. The TypeScript definition is generated\nby ts-rs — do not edit the generated file directly.",
        "required": [
          "branch",
          "display_label"
        ],
        "properties": {
          "agent_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "Agent ID working on this task"
          },
          "branch": {
            "type": "string",
            "description": "Branch name (the join key)"
          },
          "display_label": {
            "type": "string",
            "description": "Human-readable label: \"issue:42 (branch) [role]\" or \"branch [role]\""
          },
          "is_dirty": {
            "type": [
              "boolean",
              "null"
            ],
            "description": "Whether the worktree has uncommitted changes"
          },
          "issue": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Associated GitHub issue number",
            "minimum": 0
          },
          "milestones": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Milestone"
            },
            "description": "Milestone history"
          },
          "pr": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Associated pull request number",
            "minimum": 0
          },
          "review_agent_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "Review agent ID"
          },
          "worktree_path": {
            "type": [
              "string",
              "null"
            ],
            "description": "Worktree path (from git)"
          }
        }
      },
      "TaskStatus": {
        "type": "string",
        "description": "Status of a team task",
        "enum": [
          "pending",
          "in_progress",
          "completed"
        ]
      },
      "TeamMemberInfo": {
        "type": "object",
        "description": "Individual member within a team snapshot",
        "required": [
          "name",
          "agent_id",
          "is_lead"
        ],
        "properties": {
          "agent_id": {
            "type": "string",
            "description": "Unique agent identifier (UUID)"
          },
          "agent_target": {
            "type": [
              "string",
              "null"
            ],
            "description": "Current tmux pane target for this member"
          },
          "cwd": {
            "type": [
              "string",
              "null"
            ],
            "description": "Working directory of the member"
          },
          "is_lead": {
            "type": "boolean",
            "description": "Whether this member is the team lead (first member in config)"
          },
          "name": {
            "type": "string",
            "description": "Human-readable member name"
          },
          "role": {
            "type": [
              "string",
              "null"
            ],
            "description": "Role/type of the agent (from agentType in config)"
          }
        }
      },
      "TeamSnapshot": {
        "type": "object",
        "description": "Unified public snapshot of a team's state\n\nMerges the former `TeamSummary` (counts + names) with full member info\nand task list. Use this type for all new consumers.",
        "required": [
          "name",
          "members",
          "tasks",
          "last_scan",
          "task_done",
          "task_total",
          "task_in_progress",
          "task_pending",
          "worktree_names"
        ],
        "properties": {
          "description": {
            "type": [
              "string",
              "null"
            ],
            "description": "Team description"
          },
          "last_scan": {
            "type": "string",
            "format": "date-time",
            "description": "When this snapshot was last scanned"
          },
          "members": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/TeamMemberInfo"
            },
            "description": "Full member list with agent targets and roles"
          },
          "name": {
            "type": "string",
            "description": "Team name"
          },
          "task_done": {
            "type": "integer",
            "format": "int32",
            "description": "Completed task count",
            "minimum": 0
          },
          "task_in_progress": {
            "type": "integer",
            "format": "int32",
            "description": "In-progress task count",
            "minimum": 0
          },
          "task_pending": {
            "type": "integer",
            "format": "int32",
            "description": "Pending task count",
            "minimum": 0
          },
          "task_total": {
            "type": "integer",
            "format": "int32",
            "description": "Total task count",
            "minimum": 0
          },
          "tasks": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/TeamTaskInfo"
            },
            "description": "Current task list"
          },
          "worktree_names": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Worktree names used by this team's members"
          }
        }
      },
      "TeamTaskInfo": {
        "type": "object",
        "description": "Summary of team tasks (includes full task data)",
        "required": [
          "id",
          "subject",
          "status",
          "description"
        ],
        "properties": {
          "active_form": {
            "type": [
              "string",
              "null"
            ],
            "description": "Active form (present continuous verb)"
          },
          "description": {
            "type": "string",
            "description": "Task description"
          },
          "id": {
            "type": "string",
            "description": "Task ID"
          },
          "owner": {
            "type": [
              "string",
              "null"
            ],
            "description": "Task owner"
          },
          "status": {
            "$ref": "#/components/schemas/TaskStatus",
            "description": "Task status"
          },
          "subject": {
            "type": "string",
            "description": "Task subject"
          }
        }
      },
      "TerminalSubscription": {
        "type": "object",
        "description": "Owned response for `POST /api/agents/{id}/subscribe-terminal`.\n\nMirrors `tmai_pty_server::proto::TerminalTicket` augmented with an\noptional `stream_endpoint` describing where the UI should connect.",
        "required": [
          "agent_id",
          "token",
          "issued_at",
          "expires_at"
        ],
        "properties": {
          "agent_id": {
            "type": "string",
            "description": "Canonical agent id (`<scheme>:<id>`) the ticket is bound to."
          },
          "expires_at": {
            "type": "string",
            "format": "date-time",
            "description": "Wall-clock expiry time (RFC3339). UIs re-request before this."
          },
          "issued_at": {
            "type": "string",
            "format": "date-time",
            "description": "Wall-clock issuance time (RFC3339)."
          },
          "stream_endpoint": {
            "type": [
              "string",
              "null"
            ],
            "description": "Relative URL of the WebSocket-to-Unix bridge for browser UIs\n(Phase 2b). UIs `WebSocket(<server>/<stream_endpoint>?ticket=<token>&mode=stream|keys)`\nto receive ANSI bytes (or send key bytes) over a binary frame\nstream. `None` only when running against a Phase 2a-era server\nthat has not yet wired the bridge — UIs MUST tolerate `None` and\neither fall back to the legacy `/api/agents/{id}/terminal` WS or\nconnect to the local `terminal.sock` directly."
          },
          "token": {
            "type": "string",
            "description": "Opaque bearer token. UI presents this as the first frame on the\nterminal-plane socket. Treat as a credential."
          }
        }
      },
      "TmaiError": {
        "type": "object",
        "description": "Structured error carried across every tmai contract boundary.\n\nThe shape is stable: `code` is the primary dispatch key; `message` is\nhuman-readable; `retry_hint` is advisory; `context` is per-code\nstructured detail; `trace_id` ties the error to a `tracing` span for\ncross-surface correlation.",
        "required": [
          "code",
          "message"
        ],
        "properties": {
          "code": {
            "$ref": "#/components/schemas/ErrorCode",
            "description": "Machine-readable, stable error classification."
          },
          "context": {
            "type": "object",
            "description": "Code-specific structured detail. Always an object in practice; defaults\nto `null` when the caller did not attach any.\n\nWire: omitted when null (see `skip_serializing_if`). TS: rendered as\n`unknown`, which already accepts `undefined` at the call site — no\n`ts(optional)` because that attribute rejects non-`Option<T>` fields."
          },
          "message": {
            "type": "string",
            "description": "Human-readable summary. English-only in v1 (shape supports future i18n)."
          },
          "retry_hint": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/RetryHint",
                "description": "Advisory retry guidance; absent when the caller has no way to retry."
              }
            ]
          },
          "trace_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "Request/span identifier for correlating this error across MCP, WebUI,\nand internal tracing spans."
          }
        }
      },
      "TriageVerdict": {
        "type": "string",
        "description": "How a synthesis-pass triage routed a single input (worker friction note,\ndrift-audit verdict, etc.). Matches DR §B.1's three-way routing.",
        "enum": [
          "absorb",
          "escalate",
          "tradeoff_proposal"
        ]
      },
      "UnitPrsResponse": {
        "type": "object",
        "description": "`GET /api/units/{unit}/prs` response — the unit's open PRs across every\nrepo in `repos[]`, grouped per repo (primary first, then declaration\norder). A unified cross-repo surface per DR §A: the React side renders\none flat list, not a per-repo switcher.\n\nFor a single-repo unit this collapses to `repos.len() == 1`.",
        "required": [
          "unit",
          "repos"
        ],
        "properties": {
          "repos": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RepoPrsWire"
            }
          },
          "unit": {
            "type": "string"
          }
        }
      },
      "Vendor": {
        "type": "string",
        "description": "Vendor CLI identifier for dispatched agents.",
        "enum": [
          "claude",
          "codex",
          "gemini"
        ]
      },
      "VendorAvailabilityState": {
        "oneOf": [
          {
            "type": "object",
            "description": "Vendor is accepting dispatches.",
            "required": [
              "state"
            ],
            "properties": {
              "state": {
                "type": "string",
                "enum": [
                  "available"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Rate-limited; retry after `resume_at` if present.",
            "required": [
              "state"
            ],
            "properties": {
              "resume_at": {
                "type": [
                  "string",
                  "null"
                ],
                "format": "date-time",
                "description": "Wall-clock time when dispatches may resume (if known)."
              },
              "state": {
                "type": "string",
                "enum": [
                  "rate_limited"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Vendor is fully unavailable (outage, auth failure).",
            "required": [
              "reason",
              "state"
            ],
            "properties": {
              "reason": {
                "type": "string",
                "description": "Free-text reason for the outage (no secrets)."
              },
              "state": {
                "type": "string",
                "enum": [
                  "unavailable"
                ]
              }
            }
          }
        ],
        "description": "Vendor availability rollup reported by\n[`CoreEvent::VendorAvailabilityChanged`].\n\n`Available` → normal operation.\n`RateLimited` → vendor returned 429 / Max-plan limit; `resume_at` is the\nadvertised reset time when known.\n`Unavailable` → outage or auth failure; retry left to policy."
      },
      "VendorInfo": {
        "type": "object",
        "description": "Availability information for a single AI vendor.",
        "required": [
          "vendor",
          "state",
          "last_changed_at"
        ],
        "properties": {
          "account": {
            "type": [
              "string",
              "null"
            ],
            "description": "Account label when the deployment distinguishes multiple accounts."
          },
          "last_changed_at": {
            "type": "string",
            "format": "date-time",
            "description": "When the availability state last changed."
          },
          "state": {
            "$ref": "#/components/schemas/VendorAvailabilityState",
            "description": "Current availability state."
          },
          "vendor": {
            "type": "string",
            "description": "Vendor identifier (e.g. `\"anthropic\"`, `\"openai\"`)."
          }
        }
      },
      "WorkflowSnapshot": {
        "type": "object",
        "description": "UI-facing snapshot of workflow-level orchestration state.\n\nThe per-event `notify_mode_summary` / `orchestrator_notify_backlog` were\nremoved with the legacy observe-based auto-wake host (2026-05-26 amendment\nto the producer-worker-identity approach, §rip): the\n`orchestrator_notify`-derived echo taxonomy and busy-notify buffer that\nfed them are gone. What survives is the orchestrator-identity projection.",
        "properties": {
          "orchestrator_agent_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "Stable agent id of the active orchestrator, if one is running."
          }
        }
      },
      "WorkingWithHumanResponse": {
        "type": "object",
        "description": "`GET /api/units/{unit}/working-with-human` response.\n\n`dir = None` → the unit has no resolvable memory directory (no\noverride, no auto-derived `~/.claude/projects/<slug>/memory`). The\nsection reads \"no memory dir configured for this unit\" rather than\nfabricating a synthetic baseline. `dir = Some` + `memory_index =\nNone` → the directory exists but contains no `MEMORY.md`; the\nsection reads \"(no `MEMORY.md` found under <dir>)\".",
        "required": [
          "unit"
        ],
        "properties": {
          "dir": {
            "type": [
              "string",
              "null"
            ],
            "description": "Absolute path of the resolved memory directory (display-only).\n`None` when no memory dir exists for this unit."
          },
          "memory_index": {
            "type": [
              "string",
              "null"
            ],
            "description": "Raw `MEMORY.md` contents — markdown index pointing at per-entry\nmemory files. `None` when no `MEMORY.md` is present in `dir`."
          },
          "unit": {
            "type": "string"
          }
        }
      },
      "WorktreeInfo": {
        "type": "object",
        "description": "Worktree information attached to hook events in `--worktree` sessions\n\nContains name, path, branch, and the original repo directory.\nAdded in Claude Code v2.1.69.",
        "properties": {
          "branch": {
            "type": [
              "string",
              "null"
            ],
            "description": "Branch checked out in the worktree"
          },
          "name": {
            "type": [
              "string",
              "null"
            ],
            "description": "Worktree name (e.g., \"feat-auth\")"
          },
          "original_repo": {
            "type": [
              "string",
              "null"
            ],
            "description": "Original (main) repository directory"
          },
          "path": {
            "type": [
              "string",
              "null"
            ],
            "description": "Worktree filesystem path"
          }
        }
      },
      "WorktreeSnapshot": {
        "type": "object",
        "description": "Owned snapshot of a worktree for API consumers",
        "required": [
          "repo_name",
          "repo_path",
          "name",
          "path",
          "is_main"
        ],
        "properties": {
          "agent_pending": {
            "type": "boolean",
            "description": "Whether an agent is pending detection (recently spawned, not yet linked)"
          },
          "agent_target": {
            "type": [
              "string",
              "null"
            ],
            "description": "Linked agent target"
          },
          "branch": {
            "type": [
              "string",
              "null"
            ],
            "description": "Branch name"
          },
          "diff_summary": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/DiffSummarySnapshot",
                "description": "Diff statistics vs base branch"
              }
            ]
          },
          "is_dirty": {
            "type": [
              "boolean",
              "null"
            ],
            "description": "Whether this worktree has uncommitted changes"
          },
          "is_main": {
            "type": "boolean",
            "description": "Whether this is the main working tree"
          },
          "linked_dispatch_id": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/DispatchId",
                "description": "Active non-terminal dispatch working in this worktree"
              }
            ]
          },
          "linked_task_meta_branch": {
            "type": [
              "string",
              "null"
            ],
            "description": "Branch of the TaskMeta entry tied to this worktree (None for main worktrees)"
          },
          "name": {
            "type": "string",
            "description": "Worktree name"
          },
          "path": {
            "type": "string",
            "description": "Absolute path to the worktree"
          },
          "repo_name": {
            "type": "string",
            "description": "Repository name"
          },
          "repo_path": {
            "type": "string",
            "description": "Absolute path to the git common directory"
          }
        }
      }
    }
  },
  "tags": [
    {
      "name": "task-meta",
      "description": "Project task metadata — merges git worktree info with `.task-meta/` JSON"
    },
    {
      "name": "terminal",
      "description": "Terminal-plane subscription tickets (#174 — UI ↔ PTY-server direct ANSI)"
    },
    {
      "name": "calibration",
      "description": "Producer hit-rate / tier-1 tripwire view (DR 2026-05-13-synthesis-processing-and-calibration-schema §B.3)"
    },
    {
      "name": "decisions",
      "description": "Settled decisions view — bucketed projection of compose() (DR 2026-05-11-producer-conversation-workbench §1)"
    },
    {
      "name": "approaches",
      "description": "Active approaches view — peer of decisions per DR 2026-05-15-records-architecture-two-axes; surfaces status:active records with their serves / signal / review-trigger fields"
    },
    {
      "name": "working-with-human",
      "description": "Working-with-human view — memory dir + MEMORY.md projection of compose() (DR 2026-05-11-producer-conversation-workbench §1)"
    },
    {
      "name": "prs",
      "description": "Stage-1 in-tmai dev-loop: unit-scoped multi-repo PR list + PR code-diff (DR 2026-05-16-dev-loop-completes-in-tmai §A/§B)"
    }
  ]
}
