// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.

/**
 * Per-agent context-window usage derived from Claude Code's statusline
 * JSON payload (`POST /hooks/statusline`).
 *
 * The handoff-lifecycle DR §B / §F (`2026-05-14-handoff-lifecycle-and-kill-ux.md`)
 * drives the auto-handoff ritual off `pct` crossing a threshold. The `used`
 * / `total` companions are kept on the wire so the React header can render
 * `ctx: <used>k / <total>k (<pct>%)` without re-computing from the raw
 * statusline payload.
 *
 * `used` is approximated as `total * pct / 100` (CC's statusline JSON
 * reports `used_percentage` as the authoritative percent; the absolute
 * token count it ships is per-API-call, not session-cumulative — see
 * `StatuslineCurrentUsage` in `crate::hooks::types`). Consumers needing
 * exact token counts should read the transcript instead.
 */
export type AgentCtxUsage = { 
/**
 * Approximate context tokens consumed (`total * pct / 100`).
 */
used: bigint, 
/**
 * Model's context window size in tokens (e.g. 200_000).
 */
total: bigint, 
/**
 * Percentage of context window consumed (0..=100). Authoritative.
 */
pct: number, 
/**
 * When this payload was last received from CC.
 */
updated_at: string, };
