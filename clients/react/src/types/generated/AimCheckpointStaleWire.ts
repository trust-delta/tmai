// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.

/**
 * Wall-clock checkpoint-staleness for one aim node — the wire projection of
 * [`crate::workbench::compose::AimCheckpointStaleItem`]. A fourth type, separate
 * from [`AimDriftWire`] / [`AimWorkingDeltaWire`], because it states a different
 * KIND of fact: those two are aim⊥aim (corpus order / working-tree presence),
 * this one is aim⊥code — how far the repo has moved past the checkpoint the
 * operator last verified the node's claim at. The footprint is deliberately NOT
 * narrowed to the aim's own code (the provenance join is deferred), so this is a
 * *candidate to re-verify*, never a drift verdict — the verdict stays the
 * human's done-frontier (aim `aim-code-drift`).
 */
export type AimCheckpointStaleWire = { 
/**
 * The `last-verified` checkpoint the staleness is measured from (short sha).
 */
checkpoint_sha: string, 
/**
 * How many commits the repo has moved since that checkpoint. Always at or
 * above the VoI floor — below-floor and unresolvable checkpoints never
 * reach the wire.
 */
commits_since: number, };
