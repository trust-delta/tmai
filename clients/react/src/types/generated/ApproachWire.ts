// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ApproachStatus } from "./ApproachStatus";
import type { Confidence } from "./Confidence";
import type { ReviewExtensionWire } from "./ReviewExtensionWire";
import type { ReviewTriggerWire } from "./ReviewTriggerWire";

/**
 * One approach projected to the wire — the human-side complement of the
 * internal [`crate::workbench::compose::ApproachView`].
 */
export type ApproachWire = { slug: string, title: string, 
/**
 * ISO-8601 date parsed from the slug's `YYYY-MM-DD-` prefix — the
 * creation date. Approaches don't carry a `last_verified` field;
 * re-evaluation is signal-driven via `review_triggers`.
 */
date: string, status: ApproachStatus, governs: Array<string>, 
/**
 * `serves:` — non-empty per parse-time validation.
 */
serves: Array<string>, success_signal: string, failure_signal: string, 
/**
 * Re-evaluation triggers, non-empty per parse-time validation. The
 * single-date `review_after:` sugar is already expanded into a `Date`
 * trigger by the parser.
 */
review_triggers: Array<ReviewTriggerWire>, 
/**
 * Extend ledger — empty for fresh approaches.
 */
review_history: Array<ReviewExtensionWire>, 
/**
 * Producer confidence; `None` when no confidence is on record.
 */
confidence: Confidence | null, 
/**
 * Approach slugs that replaced this one — meaningful only when
 * `status: replaced`.
 */
replaced_by: Array<string>, 
/**
 * First-paragraph summary of the body.
 */
excerpt: string, };
