// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ActionOrigin } from "./ActionOrigin";
import type { EntityChange } from "./EntityChange";
import type { HandoffRitualEvent } from "./HandoffRitualEvent";

/**
 * Events emitted by the core when state changes occur.
 *
 * Consumers call [`TmaiCore::subscribe()`] to receive these events
 * via a `broadcast::Receiver`.
 *
 * This enum is the single source of truth for SSE event shapes —
 * the TypeScript discriminated union in
 * `crates/tmai-app/web/src/types/generated/CoreEvent.ts` is generated
 * from this definition by ts-rs (#446). Do not edit that file by hand.
 */
export type CoreEvent = { "type": "ConfigChanged", 
/**
 * Agent target ID
 */
target: string, 
/**
 * Config source (e.g., "user_settings", "project_settings")
 */
source: string, 
/**
 * Changed file path
 */
file_path: string, } | { "type": "InstructionsLoaded", 
/**
 * Agent target ID
 */
target: string, } | { "type": "AgentStopped", 
/**
 * Agent target ID
 */
target: string, 
/**
 * Working directory
 */
cwd: string, 
/**
 * Last assistant message (if available)
 */
last_assistant_message: string | null, } | { "type": "AgentIdPromoted", 
/**
 * Agent target ID (tmux pane string)
 */
target: string, 
/**
 * Previous id, always a `provisional:<uuid>` in Phase 1
 */
from: string, 
/**
 * New native id (`claude:<uuid>` / `codex:<uuid>` / etc.)
 */
to: string, } | { "type": "ContextCompacting", 
/**
 * Agent target ID
 */
target: string, 
/**
 * How many compactions have occurred in this session
 */
compaction_count: number, } | { "type": "WorktreeSetupCompleted", 
/**
 * Absolute path to the worktree
 */
worktree_path: string, 
/**
 * Branch name
 */
branch: string, } | { "type": "WorktreeSetupFailed", 
/**
 * Absolute path to the worktree
 */
worktree_path: string, 
/**
 * Branch name
 */
branch: string, 
/**
 * Error message
 */
error: string, } | { "type": "PromptReady", 
/**
 * Agent target ID
 */
target: string, 
/**
 * The prompt text to send
 */
prompt: string, } | { "type": "RebaseSucceeded", 
/**
 * Branch that was rebased
 */
branch: string, 
/**
 * Worktree path
 */
worktree_path: string, } | { "type": "RebaseConflict", 
/**
 * Branch that had conflicts
 */
branch: string, 
/**
 * Worktree path
 */
worktree_path: string, 
/**
 * Error details
 */
error: string, } | { "type": "PrCreated", 
/**
 * PR number
 */
pr_number: bigint, 
/**
 * PR title
 */
title: string, 
/**
 * Head branch name
 */
branch: string, } | { "type": "PrCiPassed", 
/**
 * PR number
 */
pr_number: bigint, 
/**
 * PR title
 */
title: string, 
/**
 * Summary of passed checks
 */
checks_summary: string, } | { "type": "PrCiFailed", 
/**
 * PR number
 */
pr_number: bigint, 
/**
 * PR title
 */
title: string, 
/**
 * Details of failed checks
 */
failed_details: string, } | { "type": "PrReviewFeedback", 
/**
 * PR number
 */
pr_number: bigint, 
/**
 * PR title
 */
title: string, 
/**
 * Summary of review comments
 */
comments_summary: string, 
/**
 * Monotonic review count from PR state — distinguishes
 * back-to-back ChangesRequested transitions so the notifier
 * can dedupe on `(pr_number, review_count)` instead of
 * collapsing distinct rounds into one entry.
 */
review_count: bigint, 
/**
 * Head branch name. `#[serde(default)]` keeps the field backward-
 * compatible on the wire, matching `PrCreated`/`PrClosed` which
 * already carry `branch`.
 */
branch: string, } | { "type": "PrClosed", 
/**
 * PR number
 */
pr_number: bigint, 
/**
 * PR title
 */
title: string, 
/**
 * Head branch name
 */
branch: string, } | { "type": "PrCiStartTimeout", 
/**
 * PR number
 */
pr_number: bigint, 
/**
 * PR title
 */
title: string, 
/**
 * Head branch name
 */
branch: string, 
/**
 * How many seconds the monitor has been watching this PR with no
 * CI status. Always >= the configured timeout.
 */
waited_secs: bigint, } | { "type": "GitStateChanged", 
/**
 * Repository directory path
 */
repo: string, } | { "type": "AgentTargetChanged", 
/**
 * Previous target ID (e.g., "main:0.2")
 */
old_target: string, 
/**
 * New target ID (e.g., "main:0.1")
 */
new_target: string, 
/**
 * Process ID that links the two targets
 */
pid: number, } | { "type": "ActionPerformed", 
/**
 * Who initiated the action
 */
origin: ActionOrigin, 
/**
 * API action name (e.g., "spawn_worktree", "kill_agent", "merge_pr")
 */
action: string, 
/**
 * Human-readable summary of the action
 */
summary: string, } | { "type": "AgentUpdate", 
/**
 * Whether the agent was upserted or removed.
 */
change: EntityChange, 
/**
 * Canonical AgentId (`<scheme>:<id>` form, matches `AgentSnapshot.id`).
 * Stable across tmux pane renumber, `claude --resume`, tmai restart, and
 * process restart. Provisional ids (`provisional:<uuid>`) are normally
 * hidden but become wire-visible when the agent has a `pty_session_id`
 * (PTY-server backed spawn).
 */
id: string, 
/**
 * Serialized [`super::types::AgentSnapshot`]; `None` when removed.
 */
snapshot?: unknown, 
/**
 * Who or what triggered this update.
 */
origin: ActionOrigin | null, } | { "type": "DispatchUpdate", 
/**
 * Whether the dispatch was upserted or removed.
 */
change: EntityChange, 
/**
 * Hyphenated dispatch id string.
 */
id: string, 
/**
 * Serialized [`super::snapshot::DispatchSnapshot`]; `None` when removed.
 */
snapshot?: unknown, 
/**
 * Who or what triggered this update.
 */
origin: ActionOrigin | null, } | { "type": "WorktreeUpdate", 
/**
 * Whether the worktree was upserted or removed.
 */
change: EntityChange, 
/**
 * Worktree path (unique per repo).
 */
id: string, 
/**
 * Serialized [`super::types::WorktreeSnapshot`]; `None` when removed.
 */
snapshot?: unknown, 
/**
 * Who or what triggered this update.
 */
origin: ActionOrigin | null, } | { "type": "QueueUpdate", 
/**
 * Serialized full [`super::types::QueueSnapshot`] after the mutation.
 */
snapshot: unknown, 
/**
 * Who or what triggered this update.
 */
origin: ActionOrigin | null, } | { "type": "RuntimeUpdate", 
/**
 * Serialized full [`super::snapshot::RuntimeSnapshot`].
 */
snapshot: unknown, 
/**
 * Who or what triggered this update.
 */
origin: ActionOrigin | null, } | { "type": "HandoffRitual" } & HandoffRitualEvent;
