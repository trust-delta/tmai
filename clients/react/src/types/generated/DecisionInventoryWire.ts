// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ApproachInventoryWire } from "./ApproachInventoryWire";
import type { DecisionStatus } from "./DecisionStatus";
import type { ServingHealthWire } from "./ServingHealthWire";

/**
 * One decision's projected inventory row with its serving approaches nested,
 * mirroring [`DecisionEntry`].
 *
 * `frontmatter_status` is the decision's *frontmatter* lifecycle status
 * (`draft` / `proposed` / `accepted` / …) — orienting context, NOT a fact
 * projection. The #484 engine projects status from facts for *approaches*
 * only; a decision's only status is the asserted frontmatter one, so this
 * wire field carries it under its honest name rather than mislabelling it a
 * projection.
 */
export type DecisionInventoryWire = { 
/**
 * Full record slug (`YYYY-MM-DD-…`).
 */
slug: string, 
/**
 * Slug with the `YYYY-MM-DD-` prefix stripped, for compact display.
 */
display: string, 
/**
 * The decision's frontmatter lifecycle status (display context).
 */
frontmatter_status: DecisionStatus, 
/**
 * Running-means verdict over the serving set.
 */
serving_health: ServingHealthWire, 
/**
 * How many serving approaches project to `running` (feeds the `overflow`
 * render; a mechanical count, not an appraisal).
 */
running_count: number, 
/**
 * Approaches whose `serves:` names this decision, projected + nested.
 */
serving: Array<ApproachInventoryWire>, };
