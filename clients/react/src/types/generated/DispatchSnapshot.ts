// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { BundleId } from "./BundleId";
import type { DispatchId } from "./DispatchId";
import type { DispatchKind } from "./DispatchKind";
import type { DispatchRefs } from "./DispatchRefs";
import type { DispatchState } from "./DispatchState";
import type { SpawnRole } from "./SpawnRole";
import type { SpawnerHandle } from "./SpawnerHandle";

/**
 * UI-facing snapshot of a dispatched unit of work.
 */
export type DispatchSnapshot = { 
/**
 * Durable dispatch id (UUID v7).
 */
id: DispatchId, 
/**
 * What kind of work was dispatched.
 */
kind: DispatchKind, 
/**
 * Stable references captured at request time.
 */
refs: DispatchRefs, 
/**
 * Current lifecycle state.
 */
state: DispatchState, 
/**
 * Agent role this dispatch was filled with.
 */
role: SpawnRole, 
/**
 * Who or what originated this dispatch.
 */
spawner: SpawnerHandle, 
/**
 * Wall-clock creation time.
 */
spawned_at: string, 
/**
 * Wall-clock time when the dispatch reached a terminal state, if any.
 */
finished_at?: string, 
/**
 * Live agent tmux target (e.g. "main:0.1"), if the agent has been attached.
 */
agent_target?: string, 
/**
 * Stable agent id (8-char hex), if the agent has been attached.
 */
agent_stable_id?: string, 
/**
 * PTY session id, if the agent was spawned via the PTY layer.
 */
pty_session_id?: string, 
/**
 * Bundle id when this dispatch is part of a multi-repo bundle.
 */
bundle_id?: BundleId, 
/**
 * Human-readable title chosen at dispatch time.
 */
title: string, 
/**
 * Short subtitle derived from dispatch kind (e.g. "Issue #42").
 */
display_subtitle: string, 
/**
 * Elapsed milliseconds from spawn to terminal state, if finished.
 */
duration_ms?: bigint, 
/**
 * Whether the dispatch has reached a terminal state (Stopped / Failed).
 */
is_terminal: boolean, };
