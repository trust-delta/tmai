// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ActionOrigin } from "./ActionOrigin";
import type { EntityChange } from "./EntityChange";

/**
 * SSE wire envelope for a single entity state change (Option A, full snapshot).
 *
 * `snapshot` is `None` when `change == Removed`; otherwise it is the
 * complete current state of the entity at the moment of emission.
 *
 * The `entity` field identifies the domain ("Agent", "Dispatch", …) so
 * TypeScript consumers can narrow the snapshot shape with a discriminated
 * union on `entity`.
 */
export type EntityUpdateEnvelope = { 
/**
 * Envelope schema version. Always `1` in Phase 1.
 */
v: number, 
/**
 * Event name (e.g. `"AgentUpdate"`, `"DispatchUpdate"`).
 */
event: string, 
/**
 * Whether the entity was upserted or removed.
 */
change: EntityChange, 
/**
 * Domain name (e.g. `"Agent"`, `"Dispatch"`).
 */
entity: string, 
/**
 * Stable entity identifier within the domain. For `entity == "Agent"`
 * this is the canonical AgentId (`<scheme>:<id>` form, matches
 * `AgentSnapshot.id`); for other entities (Dispatch, Worktree, Queue,
 * ...) it is the corresponding domain-native id.
 */
id: string, 
/**
 * Full entity snapshot; `null` when `change == Removed`.
 */
snapshot?: unknown, 
/**
 * Who or what triggered this change.
 */
origin?: ActionOrigin, 
/**
 * Monotonically increasing server-wide event sequence number.
 */
seq: bigint, 
/**
 * Wall-clock timestamp of emission.
 */
ts: string, };
