// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ObservationStatus } from "./ObservationStatus";

/**
 * One observation projected to the wire — the human-side complement of the
 * internal [`crate::workbench::observation::ObservationRecord`]. Carries only
 * the R-panel row + attention-marker fields; the body is not surfaced.
 */
export type ObservationWire = { 
/**
 * Filename stem — the attention key's `id` (section = `observation`).
 */
slug: string, 
/**
 * Frontmatter `summary` — the scan index.
 */
summary: string, 
/**
 * Frontmatter `status` — the appraisal `high | medium | low`.
 */
status: ObservationStatus, };
