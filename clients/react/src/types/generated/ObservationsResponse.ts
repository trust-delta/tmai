// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { RepoObservationsWire } from "./RepoObservationsWire";

/**
 * `GET /api/units/{unit}/observations` response — the unit's observations
 * surface, per repo, JSON-projected. Composed on demand (no cache). Carries
 * every observation record regardless of `status`; the R panel filters /
 * sorts client-side, mirroring the approaches / decisions dashboards.
 *
 * For a single-repo unit this collapses to `repos.len() == 1`.
 */
export type ObservationsResponse = { unit: string, 
/**
 * RFC3339 compose timestamp.
 */
composed_at: string, repos: Array<RepoObservationsWire>, };
