// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.

/**
 * Per-unit producer-feed status — the **contract-surface** the WebUI polls to
 * enable/disable the operator-trigger "差分確認" button (2026-05-26 amendment
 * §wire). Carries the mechanical delta-gate plus the raw bookkeeping it is
 * computed from, so the "tip > last_served" judgment is directly auditable on
 * the wire (the §2 "mechanical, observe-substrate zero" claim made checkable).
 */
export type ProducerFeedStatus = { 
/**
 * The unit this status is for.
 */
unit: string, 
/**
 * The unit's Producer address — `<unit>.producer` (pure naming function).
 */
producer_address: string, 
/**
 * Engine tip: highest seq ever assigned to this unit's feed (`next_seq`).
 * 0 ⇒ nothing recorded yet. Engine bookkeeping, not Producer-observed.
 */
tip: bigint, 
/**
 * High-water cursor the engine last *served* for this unit.
 */
last_served_cursor: bigint, 
/**
 * The mechanical gate: `tip > last_served_cursor`. The bit the WebUI
 * reads to enable the button. Optional/lockstep-free exactly like the
 * `is_producer` contract-surface bool (#404): `default` + `skip` make it
 * absent on the wire when `false`, so a client that omits it reads
 * `false` and neither side is forced into lockstep.
 */
has_pending_delta?: boolean, };
