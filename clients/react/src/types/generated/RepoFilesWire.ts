// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { FileTreeEntryWire } from "./FileTreeEntryWire";

/**
 * One repo's file listing within a unit (one entry per `unit.repos[]`).
 */
export type RepoFilesWire = { 
/**
 * Absolute repo root path (the same `repos[]` path the decisions / PR /
 * units endpoints key on).
 */
path: string, 
/**
 * `true` for the unit's primary repo (where the Producer is launched).
 */
primary: boolean, 
/**
 * The repo's file tree (empty when the repo path is not a directory).
 */
tree: Array<FileTreeEntryWire>, };
