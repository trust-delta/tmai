// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.

/**
 * Owned response for `POST /api/agents/{id}/subscribe-terminal`.
 *
 * Mirrors `tmai_pty_server::proto::TerminalTicket` augmented with an
 * optional `stream_endpoint` describing where the UI should connect.
 */
export type TerminalSubscription = { 
/**
 * Canonical agent id (`<scheme>:<id>`) the ticket is bound to.
 */
agent_id: string, 
/**
 * Opaque bearer token. UI presents this as the first frame on the
 * terminal-plane socket. Treat as a credential.
 */
token: string, 
/**
 * Wall-clock issuance time (RFC3339).
 */
issued_at: string, 
/**
 * Wall-clock expiry time (RFC3339). UIs re-request before this.
 */
expires_at: string, 
/**
 * PTY-server endpoint hint where the UI should present the ticket.
 * `None` at Phase 2a — populated when the UI-facing listener lands
 * in Phase 2b. UIs MUST tolerate `None` and either fall back to
 * the legacy WS or use a locally-known endpoint.
 */
stream_endpoint?: string, };
