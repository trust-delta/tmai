// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ApproachInventoryWire } from "./ApproachInventoryWire";
import type { DecisionInventoryWire } from "./DecisionInventoryWire";

/**
 * `GET /api/units/{unit}/inventory` response — the unit's nested in-play
 * inventory, mirroring [`Inventory`].
 *
 * Every decision (sorted by slug, serving approaches nested) followed by the
 * unanchored approaches. `today` is the ISO reference date liveness was
 * computed against (so the R-panel can render an "as of" without guessing the
 * server clock).
 */
export type UnitInventoryResponse = { unit: string, 
/**
 * ISO `YYYY-MM-DD` reference date the liveness was computed against.
 */
today: string, decision_count: number, approach_count: number, decisions: Array<DecisionInventoryWire>, unanchored_approaches: Array<ApproachInventoryWire>, };
